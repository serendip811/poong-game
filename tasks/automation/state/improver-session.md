# Improver Session

Purpose: persistent memory for the improvement agent.

Use this file to remember:
- recent implementation patterns that worked well
- systems that are getting overbuilt and need consolidation
- important technical risks or balance risks
- short next-step heuristics for bounded, high-value improvements

Keep this short and replace stale notes instead of endlessly appending.

## Current Memory

- The shipped late staircase now needs to stay on one `Wave 8` form lock plus one automatic `Wave 10` reinforcement beat. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `applyBaseRouteWave10Reinforcement(...)` firing exactly once when a consolidated build enters `Wave 10` with `lateBreakProfileId`, promoting `mutation` to `lateFieldMutationLevel >= 5`, `aegis` to `lateFieldAegisLevel >= 3`, and `ledger` to an extended `Black Ledger` end stretch plus extra pickup/scrap pull. Keep `getBaseRouteWave8ClosureCopy(...)`, `getLateBreakCadenceSummary(...)`, `getLateBreakHeadline(...)`, and `getShippingRouteLateFinaleRoadmap(...)` naming that same `Wave 10` reinforcement beat (`Siege Feeders / Fortress Pulse / Jackpot Tow`) so the late run reads as one lock then one reinforcement, not a pile of caches or late wrappers. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the reinforcement titles, roadmap detail, mutation damage bump, ledger raid extension, and quarantine reset of `lateBreakReinforcementApplied`. If a future pass removes the automatic Wave 10 reinforcement or reopens multiple late exception beats around it, critique will correctly say the second half is structurally crowded again.
- The shipped reward/transition feed now needs to stay capstone-first and admin-light after forge picks. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteWave8ClosureCopy(...)` using only the locked form plus one immediate combat ask, `getBaseRouteTransitionFeedCopy(...)` saying `${dominantForm} 정비/정리` instead of `현재 형태는 ...`, and `handleForgeSelection(...)` describing package followups as `작은 보강 하나` instead of `rider slot`, `support/defense/greed`, or support-clause late-break recaps. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting mid-forge transition text rejects `현재 형태는`, `support`, `rider slot`, and that Wave 8 closure transition/transformation text stays off support-system names. If a future pass reintroduces support/admin wording into these first-read reward beats just because the underlying build still has rider systems installed, critique will correctly say the game is still explaining packages before selling desire.
- The shipped forge context must now stay locked to payoff-owned titles instead of raw package labels. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteForgeContextIdentity(...)` plus `renderForgeOverlay()` preferring `getBaseRouteForgeSpotlightSummary(...).titleValue` / `transformation.cardTitle` over `choice.title` when the compact-machine-read header/detail are built, so `Wave 6` context says `Twin Spine -> Crownsplitter Array` instead of falling back to chassis/package names. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the new helper resolves `Crownsplitter Array` and rejects the raw `Split-Wing Chassis` title. If a future pass reintroduces `choice.title` precedence in forge context just because the underlying card still carries a chassis/support wrapper id, critique will correctly say the reward stop still reads like package assembly instead of one irresistible form leap.
- The shipped late staircase now needs to keep route-owned `Wave 11-12` contracts instead of falling back to the shared `Refuge Run / Final Stand` shell once `lateBreakProfileId` is locked. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `LATE_BREAK_ENDGAME_PROFILES` plus `getLateBreakEndgameProfile(...)` so `mutation` resolves `Raptor Sweep -> Crown Siege`, `aegis` resolves `Bulwark Rotation -> Citadel Lock`, and `ledger` resolves `Exit Vector -> Jackpot Lock`, each with their own arena/hazard/mix tuning and labels on `resolveWaveConfig(10/11, build)`. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting those route-owned wave labels, arena sizes, and hazard profiles while leaving the no-profile shared route on `Refuge Run / Final Stand`. If a future pass lets `Wave 11-12` drift back onto shared labels/config just because `Wave 9-10` are already differentiated, critique will correctly say the second half still feels like one ladder with seasoning.
- The shipped consolidated route now needs to preserve its late-form handoff instead of collapsing back to a `Wave 8` pseudo-finale. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `isBaseRouteLateStaircaseActive(...)` / `getBaseRoutePlayableWaveCount(...)` so a chosen `lateBreakProfileId` extends the live cap from `8` to `12`, keeps `getBaseRoutePostWaveTransition(...)` on `continue_wave` for `Wave 9-12`, and lets the clear loop call `beginWave(...)` instead of routing to `beginBaseRouteVictoryLap()`. Keep the same helper feeding `renderWaveTrack()`, `createBaseRouteMachinePanelMarkup(...)`, `createBaseRoutePauseSnapshotMarkup(...)`, `getBaseRouteCombatAskForWave(...)`, `getLiveSideBetSummary(...)`, and `getShippingCombatFeedEntrySummary(...)` so late-wave HUD/readouts stop clamping back to `Wave 8`. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting a `Wave 8` capstone build promotes the route cap to `12` and transitions to `continue_wave` at `Wave 9`. If a future pass reintroduces the `Wave 8 -> victory lap/result` shortcut for capped late-form builds, critique will correctly say the game still peaks where its second half should start.
- The shipped route roadmap/result layer now needs to stay on a short `run memory` grammar instead of stage-spec wording. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteFinaleRoadmap(...)`, `getBaseRouteResultCopy(...)`, `getBaseRouteResultRouteLabel(...)`, `getBaseRouteResultBeatLabels(...)`, and `createBaseRouteResultBuildMarkup(...)` using `마감 랩`, `RUN`, and one-line form/route closure copy rather than `Wave 8 완성 시험`, `승리 랩`, or support-rider recap in the shipped default view. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the shipped result labels/roadmap titles read `마감 랩` and reject the older `RUN MEMORY` / `Wave 8 완성 시험` phrasing. If a future pass restores stage-spec or rider recap language on the result/roadmap defaults, critique will correctly say the shipped run still reads like documentation instead of one compact rerun memory.
- The shipped forge headline now needs to stay transformation-first even when a support install rides along. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteForgeSpotlightSummary(...)` and `getBaseRouteForgeDominantInstallHero(...)` preferring `transformation.cardTitle` / `previewValue` over `supportInstall.title` for `Wave 6` chassis packages, keep `renderForgeOverlay()` using generic `주력 변이` / `버팀 변화` eyebrows instead of support payoff labels, and keep doctrine `supportDoctrineText` selling `Wave 8` support as a quiet amplifier for the already locked form. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the shipped `Wave 6` forge spotlight/context now headline `Crownsplitter Array` / `Citadel Spindle` instead of `Seeker Array` / `Aegis Halo`. If a future pass lets forge default back to support titles just because those installs are bundled on the card, critique will correctly say the reward stop still sells hardware catalog over body/weapon fantasy.
- The shipped `Wave 6-8` route ladder now needs to stay off support-chapter breathing-room and on route-owned cadence. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) with `resolveWaveConfig(...)` no longer wrapping shipped waves in `applySupportChapterBreathingRoom(...)`, keep the shared cells named `dominion_sweep` / `dominion_proof` instead of `support_lap` / `support_proof`, and keep `createBaseRouteVictoryLapWave(...)` selling `body/weapon form` rather than `body/support bracket`. Keep `applyWave5FieldPathEncounterConfig(...)` making offense materially denser/faster and defense materially slower/lower-budget, and keep `applyMidrunGreedRouteConfig(...)` on lower-budget but sharper burst windows. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting offense raises `spawnBudget` and lowers `baseSpawnInterval`, defense lowers `spawnBudget` and raises `baseSpawnInterval`, greed lowers total budget but raises burst pressure, and support-only Wave 7 configs stay on base `ownership_breathing` instead of a helper-showcase hazard profile. If a future pass restores helper-owned arena breathing or flattens the three branches back onto the same cadence ladder, critique will correctly say the midrun still belongs to support framing instead of the chosen path.
- The shipped `Wave 6-8` path fork now needs to keep owning hazard placement logic, not just labels, via route-specific targeting profiles. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `HAZARD_TARGETING_PROFILES.offense_killlane`, `.defense_refuge`, and `.greed_dive_exit`, with `applyWave5FieldPathEncounterConfig(...)` stamping offense/defense hazards onto those profiles and `applyMidrunGreedRouteConfig(...)` stamping greed salvage/caravan hazards onto `greed_dive_exit`. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `Killline Relay`, `Refuge Relay`, and `Entry Snap -> Caravan Hook -> Jackpot Exit` all carry their owned targeting profile. If a future pass lets those hazards drift back to shared `ownership_breathing` just because the notes/directives still differ, critique will correctly say the three routes still collapse back toward one seam-holding exam.
- The shipped `Wave 8` closure now needs to stay capstone-first on every player-facing surface instead of letting the already installed support silhouette steal the headline. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteWave8ClosureCopy(...)` always naming `Cataclysm Arsenal`, `Warplate Halo`, or `Black Ledger Heist` as the title, with installed support only appended as reinforcement detail inside copy/feed. Keep `getBaseRouteForgeChoiceTransformation(...)`, `getBaseRouteForgeSpotlightSummary(...)`, `getBaseRouteForgeDominantInstallHero(...)`, and `getLateBreakRoutePreview(...)` prioritizing `완성 형태` plus the branch-owned capstone title during the shipped `nextWave: 8` forge. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the defense/greed Wave 8 spotlight, hero title, and preview rows read `Warplate Halo` / `Black Ledger Heist` under `완성 형태`. If a future pass restores `Aegis Halo`, `Ember Ring`, or other support names as the Wave 8 headline just because that installed toy is already present, critique will correctly say the run still closes on helper memory instead of the path-owned body/weapon fusion.
- The shipped forge context now needs to stay on the same one-glance `compact-machine-read` grammar as pause/detail instead of reopening the older `forge-context-rail`. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createBaseRouteForgeContextMarkup(...)` returning `createCompactMachineReadMarkup(...)` with one dominant reward title, one muted transition line (`Prism Crown -> Dominion Sweep`, `Afterglow -> 판돈 급습`, etc.), and one explicit `다음 전투` ask wired from `renderForgeOverlay()`. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting forge context contains `compact-machine-read__ask` and the live ask text while rejecting the older rail/admin shell. If a future pass restores two equal-weight rail slots or lets forge context omit the next-combat ask again just to show more route metadata, critique will correctly say the reward stop is still explaining structure instead of showing one object, one payoff, one next ask.
- The shipped pause snapshot and `Tab` machine panel now need to stay on the same one-glance `compact-machine-read` shell instead of drifting back to pills, spotlight cards, or side-by-side status grids. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createCompactMachineReadMarkup(...)` driving both `createBaseRouteMachinePanelMarkup(...)` and `createBaseRoutePauseSnapshotMarkup(...)`, with only `현재 머신` as the dominant title, one muted payoff line (`Wave 5 경로 · 방호 고정`, `첫 도약 · Wave 3 무기 방향`, etc.), and one `다음 전투` ask block. Keep [playables/cinder-circuit/styles.css](/Users/seren/workspace/poong-game/playables/cinder-circuit/styles.css) on `.compact-machine-read*` as the shared visual grammar, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting pause/detail markup contains `compact-machine-read__ask` while rejecting `pause-summary__pill-row` on shipped surfaces. If a future pass restores equal-weight pills or a second spotlight card just to show more route/admin context, critique will correctly say the detail stack became a reading board again instead of one dominant silhouette plus one combat ask.
- The shipped `Wave 7` ladder now needs to stay off the `Crownfire Overdrive` wrapper so the compact run keeps breathing room after the `Wave 6` chassis lock. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createCrownfireOverdriveChoice(...)` returning `null` for the consolidated `Wave 1-8` route, which lets `buildForgeChoices(...)` surface a visible main-weapon evolution as the `Wave 7` headline instead of another named overdrive layer. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the shipped `rail_zeal` route lands on `Wave 7` weapon evolution, no `crownfireOverdriveId`, and the evolved fire pattern still applies. If a future pass reopens `Crownfire Overdrive` inside the shipped slice just to make `Wave 7` feel louder, critique will correctly say the base run is back to stacking extra wrappers before the late payoff breathes.
- The shipped `Wave 6` offense/defense breakpoint packages now need to keep support as a delayed amplifier instead of immediate spectacle. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createWave6ChassisInstallChoice(...)` stamping `systemChoice.activationWave = 8` for the compact route, `computeSupportSystemStats(build, waveNumber)` filtering out not-yet-online support entries during live combat, and the `handleForgeSelection(...)` feed explicitly telling the player the support circuit is folded away until `Wave 8` so the `Wave 6-7` proof window stays gun/body-first. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `Wave 6` packages carry `activationWave: 8`, `Wave 7` has no live support stats, and `Wave 8` turns the staged system back on. If a future pass reactivates those bundled support installs at `Wave 6` just to make the breakpoint feel fuller, critique will correctly say the run is still spending its payoff budget on helpers before the second weapon spike lands.
- The shipped `Wave 5` greed branch now needs to stay on a true `dive -> cut -> exit` ladder instead of reading like the shared ownership cell with salvage labels pasted on. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `applyMidrunGreedRouteConfig(...)` widening `Wave 6-8` into `Entry Snap -> Caravan Hook -> Jackpot Exit` with lower active caps, larger arenas, faster caravan/vault closures, and explicit `chassisProof` states, and keep the `beginWave(...)` combat feed using `midrunGreedRoute.note` so each greed wave announces its own cash-out rule instead of a generic scrap warning. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the greed route's new ask text plus arena width, active-cap, drift-speed, and payout deltas. If a future pass lets greed fall back to the old `짧게 긁고 빠진다` wrapper or re-densifies the wave instead of preserving fast exit windows, critique will correctly say the third path still is not a real movement fantasy.
- The shipped base-route forge now needs to stay on a slim status rail above one dominant hero card instead of reopening the old context stack. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createBaseRouteForgeContextMarkup(...)` rendering two compact rail items (`현재 머신`, current path/payoff) plus at most one transition note, while `createBaseRouteForgeHeadlineCardMarkup(...)` owns the only big title and `다음 전투` proof line. Keep [playables/cinder-circuit/styles.css](/Users/seren/workspace/poong-game/playables/cinder-circuit/styles.css) on `.forge-context-rail*` and the quieter `.forge-card--contract-side` treatment so side offers read as supporting options rather than equal-weight briefing cards, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `forge-context-rail`, `현재 머신`, and headline `forge-card__tag` markup. If a future pass restores a large context spotlight or adds another proof/explanation block above the cards just to make forge feel fuller, critique will correctly say the reward stop became a reading stack again instead of one dominant appetite beat.
- The shipped `Wave 8` closure now needs to stay branch-owned instead of reopening a shared generic headline pool. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteWave8CapstoneChoice(...)` forcing `Wave 5 offense -> Cataclysm Arsenal`, `defense -> Warplate Halo`, and `greed -> Black Ledger Heist` during the compact route's `nextWave: 8` forge, with `buildForgeChoices(...)` stamping that card as the `완성` headline before any support-upgrade promotion can steal the slot. Keep `createLateBreakMutationChoice(...)`, `createLateBreakAegisChoice(...)`, and `createLateBreakGreedContractChoice(...)` available on the shipped `Wave 8` stop via `canOfferLateBreakClosureChoice(...)`, and keep the post-pick feed in `selectForgeChoice(...)` firing on `DEFAULT_ROUTE_WAVE_COUNT` as well as the late armory. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting each path only sees its owned `Wave 8` capstone title. If a future pass re-promotes generic support upgrades back into the `headline` slot at `Wave 8` just because they score well, critique will correctly say the route still peaks at `Wave 6` and closes on smaller admin-tier follow-through.
- The shipped `Wave 6` path package now needs to stay on three visibly larger branch forms instead of one shared modest chassis rider. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `buildWave6ChassisBreakpointChoices(...)` granting `offense -> Vector Thrusters + core-matched late ascension`, `defense -> Bulwark Treads + Citadel Spindle`, and `greed -> Salvage Winch + Towchain Reaver`, with `applyForgeChoice(...)` immediately applying those late-form ids during the shipped route. Keep `createForgePreviewRows(...)` and `getBaseRouteForgeChoiceTransformation(...)` prioritizing the package spotlight (`Crownsplitter Array`, `Citadel Spindle`, `Towchain Reaver`, etc.) over the smaller support label on `Wave 6` cards, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the route packages and their preview labels. If a future pass demotes `Wave 6` back to support-first card language or removes the immediate late-form application just to keep the breakpoint simpler, critique will correctly say the shipped run is still hoarding its best transformations outside the playable route.
- The shipped combat HUD now needs to stay `survival cluster + compact machine read + single wave ask`, with detail hidden behind `Tab` instead of living in the default shell. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `shouldShowBaseRouteHudDetail(...)`, `getMinimalBaseRouteHudVisibility(...)`, and `renderCombatFeed()` showing `createMinimalCombatAskMarkup(...)` during live base-route combat while `Tab` temporarily reveals wave/timer/scrap/dash plus the recent feed stack. Keep [playables/cinder-circuit/styles.css](/Users/seren/workspace/poong-game/playables/cinder-circuit/styles.css) on `.combat-feed--minimal-ask` as the one default right-side card, and keep [docs/games/cinder-circuit-design.md](/Users/seren/workspace/poong-game/docs/games/cinder-circuit-design.md) describing `Tab` as the detail board. If a future pass restores stacked telemetry or passive detail to the default combat shell just to make the HUD feel fuller, critique will correctly say the arena lost breathing room again.
- The shipped result screen now needs to stay on branch memory, not completion cosplay. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createBaseRouteResultBuildMarkup(...)` and `finishRun(...)` rendering `RUN MEMORY`, the `Wave 5` route spine, and a short final-form read (`최종 형태 / 무기 축 / 잔여 고철`) instead of `FINAL FORM`, `Drive`, affix chips, bench inventory, or other admin rows on the main rerun-facing card. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the result markup includes `Wave 5 판돈 급습` and excludes `FINAL FORM`, `Drive`, `속성 없음`, and `보관 코어`. If a future pass restores late-run bookkeeping to make the result feel fuller, critique will correctly say the `Wave 8` closure still reads like a debug summary instead of a hunger loop recap.
- The shipped run-memory shell now needs to stay `Bare Hull -> Wave 3 weapon -> Wave 5 path -> Wave 8 closure`, with `Wave 6` support as a payoff detail instead of the headline. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteStatusBoardSummary(...)`, `getShippingLadderSteps(...)`, `renderForgeOverlay()`, `getBaseRouteResultRouteLabel(...)`, and `getBaseRouteResultBeatLabels(...)` surfacing `Wave 5 경로` / `공세 추적|방호 고정|판돈 급습` before any support label; keep `getBaseRoutePauseSnapshotSummary(...)` only using `현재 설치` when `payoffLabel === "설치"` so branch-first summaries do not masquerade as install-first ones. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting shipped ladder/result/machine-panel text now says `ROUTE`, `Wave 5 진로 선택`, and `Wave 8 완성 시험`. If a future pass lets shared title/result/status helpers drift back to `다음 설치` or `FINAL FORM` memory just to spotlight support toys, critique will correctly say the run still presents itself like the old prototype.
- The shipped `Wave 5` fork now needs to keep owning live `Wave 6-8` encounter shape, not just ask/history text. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `applyWave5FieldPathEncounterConfig(...)` inside `resolveWaveConfig(...)`, with `offense` rewriting `Breakline/Crownfire/Forgecross` into faster `Killline Relay -> Cinder Chase -> Breach Pocket` pressure and `defense` rewriting them into lower-cap `Refuge Relay -> Breather Drift -> Holdfast Pocket` recovery windows, while `greed` continues using `applyMidrunGreedRouteConfig(...)`. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting those branch configs diverge on hazard labels, directives, active caps, and proof labels. If a future pass lets `Wave 6-8` fall back to the same ownership wrapper again just because support/chassis proof already exists, critique will correctly say the `Wave 5` fork still does not change how the run is actually played.
- The shipped `Wave 5` fork now needs to persist as route memory, not just a temporary forge card. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `recordBaseRouteWave5FieldPath(...)` storing `offense|defense|greed` when the compact route resolves `nextWave: 5`, keep `getBaseRouteWave5FieldPathSummary(...)` feeding `getBaseRouteCompactCombatAsk(...)`, `createBaseRoutePauseSnapshotMarkup(...)`, `getBaseRouteResultCopy(...)`, and `getBaseRouteResultBeatLabels(...)`, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `Wave 5` offense/defense/greed choices rewrite `Wave 6-8` asks plus pause/result memory (`Wave 5 판돈 급습`, `방호 고정`, etc.). If a future pass lets pause/result fall back to the generic `다음 전투` / `Wave 3 -> Wave 6 -> Wave 8` ladder just to stay cleaner, critique will correctly say the new fork still reads like a temporary card instead of a run path.
- The shipped `Wave 5` early-mutation forge now needs to stay a real three-way appetite stop, not a two-card bridge into `Wave 6`. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `buildForgeChoices(...)` treating `EARLY_MUTATION_FORGE_WAVE` as a `headline + rider + gamble` fork for the compact route, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting shipped `nextWave: 5` forge/followup choices expose `headline|rider|gamble` with `field_greed` in the `판돈` slot. If a future pass compresses that stop back to two cards just to keep the opener cleaner, critique will correctly say the run still only has `Wave 3` and `Wave 6` as real appetite beats.
- The shipped `Wave 1-8` combat brief now needs to stay on short board labels, not sentence `note/directive` copy. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteWaveBriefingSummary(...)` and `getBaseRouteCompactCombatAsk(...)`, with `getImmediateProofWindowSummary(...)`, `getShippingCombatFeedEntrySummary(...)`, and the base-route `beginWave(...)` intro feed all pulling the same compact `headline + proof` pair. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `Wave 4` proof resolves to `사선 유지 / 굵은 차단선 하나만 피해 간다.` and that even a verbose `Wave 4` intro log is summarized back to that compact read. If a future pass reuses raw `resolvedWave.directive` or long wave intro text on shipped proof/feed surfaces just to add flavor, critique will correctly say the current chapter drifted back into sentence parsing instead of glanceable threat states.
- The shipped base-route forge now needs to keep its proof line concentrated on the single headline reward card, not repeated across the context block and side cards. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `renderForgeOverlay()` passing no `askNote` into `createBaseRouteForgeContextMarkup(...)`, keep `createBaseRouteForgeCompactCardMarkup(...)` free of combat-ask copy even if a caller provides one, and keep `createBaseRouteForgeHeadlineCardMarkup(...)` using `createBaseRouteForgeProofMarkup(...)` so the oversized payoff card owns the only short `다음 전투` line. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting rider cards drop passed-in ask text while headline cards still render one compact proof line. If a future pass restores the same ask text to the context card or secondary cards just to make forge feel more informative, critique will correctly say the reward stop slid back into briefing/admin layering instead of one-card desire.
- The shipped `Wave 6-8` first support install now needs to stay silhouette-first in live combat, not debug-overlay-first. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getSupportRenderPresentation(...)` suppressing orbit frames, deployable range circles, and Aegis intercept rings for the compact route's first tier-1 support install while still leaving the actual satellite/turret bodies visible. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting that base-route tier-1 support ownership at `Wave 7` returns `showOrbitFrames === false`, but a later `Mk.II` upgrade returns `showOrbitFrames === true`. If a future pass restores those guide rings just to make support tech look louder the moment it unlocks, critique will correctly say the first install reads like clutter instead of one owned lane tool.
- The shipped `Wave 1-8` HUD/status panel now needs to stay on the same `visible machine change + immediate combat ask` shell as the compact forge/pause cards. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createBaseRouteMachinePanelMarkup(...)` sanitizing the build before it renders `createCurrentMachinePayoffMarkup(...)` plus one `machine-payoff__note`, and keep both `createEraContractPanelMarkup(...)` and `renderWaveTrack()` calling that helper instead of rebuilding raw status rows from live state. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting polluted shipped builds still render the machine panel without `Afterburn`, `Wave 9`, or `Cataclysm`. If a future pass drops the note or routes wave-track back through unsanitized live summaries just to show more progression context, critique will correctly say the shipped status surface drifted back toward roadmap admin instead of one-glance chapter hunger.
- The shipped `Wave 8` support chapter now needs a hard `no catch-up install` rule: if the player reaches the compact route's finish forge without a `Wave 6` support already installed, [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) should keep `shouldBlockBaseRouteWave8SupportCatchup(...)` returning true so `shouldOfferSupportSystem(...)` and `getVisibleSupportOfferSystemIds(...)` both suppress fresh support choices instead of backfilling the silhouette at the last second. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting supportless shipped `Wave 8` builds expose no `system` choices at all. If a future pass reopens a late support catch-up just to make more runs feel complete, critique will correctly say the compact ladder stopped being `starter -> weapon -> earned support proof`.
- The shipped `Wave 1-8` clear/forge transition feed now needs to stay on one shared `current form -> offered leap -> next ask` sentence, not wave-by-wave admin copy. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteTransitionFeedCopy(...)` driving the shipped branches inside `maybeAdvancePhase(...)` and `enterForge()`, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting those summaries still include concrete beats like `첫 차체 잠금` or `짧은 승리 랩` without `Late Break`, `Afterburn`, `support bay`, or `Wave 9-10` grammar. If a future pass reintroduces wave-specific transition narration just to explain stage cadence harder, critique will correctly say the shipped reward rhythm is back to run management instead of `fight -> pick -> test`.
- The shipped `Wave 6-8` support chapter now needs `support_showcase` hazard placement to stay visibly off the player’s forward lane, not just lower in count. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `HAZARD_TARGETING_PROFILES.support_showcase` using outer-flank candidates plus `routeAvoidanceMultiplier` / `routeTagPenalty`, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting support-showcase candidates include `outer-flank-left/right` and deterministic spawns land well off the immediate route-focus line. If a future pass only lowers hazard counts again while letting `chooseHazardSpawn(...)` drift back toward forward-route anchors, critique will correctly say Wave 6-8 still denies lanes instead of letting one owned support install breathe.
- The shipped `result` and `recent gain` surfaces now need to stay on the same quarantined `starter -> Wave 3 weapon -> Wave 6 support -> Wave 8 finish` ladder as the rest of the `Wave 1-8` route. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getShippingUpgradePresentationLabel(...)` rewriting shipped `교리 채택` and `Wave 6 Ascension` history into visible machine/support labels when build context exists, and keep `finishRun(...)` using `getBaseRouteResultRouteLabel(...)` plus `getBaseRouteResultBeatLabels(...)` instead of doctrine names or raw upgrade logs. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting result labels resolve to concrete beats like `Wave 3 Ember Spindle` and `Wave 6 Seeker Array`, not `Mirror Hunt Doctrine` or `정상 기록`. If a future pass restores doctrine/admin names on result or pause history just to preserve upgrade chronology, critique will correctly say the shipped ladder is still leaking roadmap grammar after the run ends.
- The shipped `Wave 5` field grant now needs to stay a real three-way appetite check, not the old two-card safety stop. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `buildFieldGrantChoices(...)` forcing `headline / rider / gamble` when `nextWave === 5` on the consolidated `Wave 1-8` route, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the `Wave 5` `판돈` slot is a live `field_greed` card. If a future pass folds `Wave 5` back to two cards just to keep the early ladder safer, critique will correctly say the run still spends too much of its core slice on managed confirmation instead of repeated appetite tests.
- The shipped `Wave 7` field grant now needs to stay a real three-way follow-up after any `Wave 6` chassis breakpoint. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `buildFieldGrantChoices(...)` forcing a `headline / rider / gamble` contract when `nextWave === 7` and `build.wave6ChassisBreakpoint` is true, even though the rest of the shipped base route still prefers two-card field grants. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the live `Wave 7` cache after a breakpoint run shows `field_greed` as the `판돈` card next to the `Ember Ring` rider. If a future pass folds this back to two cards just to protect the support proof chapter, critique will correctly say the run still only offers one honest lane choice before collapsing into script again.
- The shipped `Wave 6` midpoint now works better when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) treats `buildWave6ChassisBreakpointChoices(...)` and the `nextWave === 6` branch inside `buildForgeChoices(...)` as a real three-way fork, not a promoted support card plus vent. Keep `wave6BranchRole` on `offense / defense / greed`, keep the live forge returning `headline = doctrine-primary support install`, `rider = doctrine-midrun support install`, and `gamble = Scrapline Raid Frame`, and keep `field_greed` able to carry a `wave6GreedFrame` chassis lock so the greed branch still earns a real body-form payoff instead of just scrap text. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting doctrine-specific `Wave 6` forks and the greed-frame crownfire path. If a future pass lets the generic two-card contract or `shouldHeadlineSupportInstall` promotion swallow this beat again, critique will correctly say the run is back to a disguised confirm screen.
- The shipped forge header now reads better when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) keeps `createBaseRouteForgeContextMarkup(...)` on a two-step object-first shell: one dominant transform title, one short `current -> result` line, and one `바로 다음 전투` ask, instead of `현재 머신 / 대표 설치 / 전투 요청` admin rows. Keep `renderForgeOverlay()` passing simple stage eyebrows like `주력 변이`, `지원 설치`, and `완성 보상`, and keep `getBaseRouteWave8ClosureCopy(...)` on short completion reads like `Cataclysm Arsenal 완성` rather than roadmap narration. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the shipped forge context shows `지금 변신` or stage eyebrows plus `A -> B` transitions and `바로 다음 전투`, not `현재 머신` or route-roadmap copy. If a future pass restores labeled admin rows just to explain builds harder, critique will correctly say the reward moment is back to run administration instead of object-first hunger.
- The shipped compact route now needs its `combat feed` to stay a status board even when `Tab`/pause expands details. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `renderCombatFeed()` using `getShippingCombatFeedEntrySummary(...)` for the shipped `Wave 1-8` route instead of dumping raw `entry.text` in expanded pause mode, and keep `shouldQuarantineShippingCombatFeedText(...)` redirecting any leaked `Afterburn / Ascension / Wave 9-12 / Dominion Run` feed text back to sanitized `getBaseRouteStatusBoardSummary(...)` plus the current combat ask. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting polluted `Dominion Run` feed copy no longer survives in the shipped summary helper. If a future pass restores raw wave-log paragraphs on the shipped pause feed just to show more flavor, critique will correctly say `Tab` is back to reading like prototype history instead of one-glance run state.
- The shipped compact route now reads cleaner when all shipped-facing status helpers keep the machine as the headline object even after a support install lands. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getShippingContractSummary(...)` projecting `getBaseRouteStatusBoardSummary(...)` back into the old `title/lead` shape, and keep `renderWaveTrack()` using that same machine-first summary instead of the older `getBaseRouteOwnedPowerSummary(...)` wording. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting shipped title/forge/wave-track summaries say `현재 머신 -> 설치` or `현재 머신 -> 다음 설치`, not `설치 -> 주력 변이` or `무기 변이`. If a future pass restores install-first labels just to make support moments feel larger, critique will correctly say the shipped run is back to reading like route bookkeeping instead of one-glance loadout plus offered change.
- The shipped compact forge now reads cleaner when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) keeps base-route cards on `title + transformation spotlight + combat ask` and does not render the extra `hero-copy` sentence inside `createBaseRouteForgeHeadlineCardMarkup(...)` or `createBaseRouteForgeCompactCardMarkup(...)`. Keep [playables/cinder-circuit/styles.css](/Users/seren/workspace/poong-game/playables/cinder-circuit/styles.css) treating `.forge-card__spotlight` as the shared card primitive for both snap and side cards, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting recurring shipped cards no longer emit `forge-card__hero-copy` or `forge-card__pivot`. If a future pass adds explanatory body text back into those shipped cards just to recover flavor, critique will correctly say the forge is sliding back into dashboard copy instead of one-glance desire.
- The shipped `Wave 8` support payoff now reads better when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) treats an installed support `Mk.II` upgrade as a distinct completion beat instead of a generic forge card. Keep `getBaseRouteWave8SupportPayoffSummary(...)`, `getBaseRouteForgeChoiceTransformation(...)`, `createBaseRouteForgeHeadlineCardMarkup(...)`, and `renderForgeOverlay()` aligned on `완성 보상 -> 현재 설치 -> 완성 형태 -> 완성 시험`, and keep [playables/cinder-circuit/styles.css](/Users/seren/workspace/poong-game/playables/cinder-circuit/styles.css) plus [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the evolution strip survives on representative `Wave 8` support-upgrade cards. If a future pass routes those cards back through the plain `대표 설치 / 전투 요청` shell just to keep surfaces uniform, critique will correctly say the back-half payoff is still a modest tier bump instead of a form the player wants to chase.
- The shipped `Wave 6-8` support chapter now holds cleaner when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) runs `applySupportChapterBreathingRoom(...)` after support-proof shaping so a real `Wave 6` install gets lighter spawn pressure and less route-targeted hazard placement through two fights. Keep `HAZARD_TARGETING_PROFILES.support_showcase`, `resolveWaveConfig(...)`, and [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) aligned on `support_showcase` plus lower `spawnBudget/activeCap` and slower hazard cadence for representative `Wave 7` support-ownership builds. If a future pass reverts those fights back to `ownership_breathing` just to restore tension, critique will correctly say the support chapter is still being judged inside route denial instead of letting the installed silhouette own space first.
- The shipped compact route now needs a hard state quarantine as well as choice gating: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `BASE_BUILD.signatureId = null` for the consolidated route, `sanitizeConsolidatedBuildState(...)` zeroing `auxiliaryJunctionLevel`, wiping wildcard/pursuit residue, and forcing `supportBayCap` back to `1`, and `shouldRunDoctrinePursuitWave(...)` returning `false` on the shipped route even for polluted states. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting a dirty compact build sanitizes back to one bay with no pursuit/wildcard carryover. If a future pass loosens this just because choice generators already hide those branches, critique will correctly say `Wave 6-8` is still one state leak away from rescue logic instead of an honest single-install chapter.
- The shipped compact route now holds the first support chapter more honestly when `Wave 6` installs one support silhouette without expanding support bay count, and when `Wave 8` only re-offers the installed support for an upgrade instead of reopening fresh installs. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `buildWave6ChassisBreakpointChoices(...)` using `singleAxisBreakpoint` without `bayUnlock`, `unlockLateSupportBay(...)` returning no-op for the consolidated route, and `getVisibleSupportOfferSystemIds(...)` resolving `Wave 8` to the already installed support id only; keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `supportBayCap` stays `1` through the shipped `Wave 6-8` path. If a future pass reopens `Wave 8` support forks or late bay uplinks just to add variety, critique will correctly say the support chapter is still being rescued instead of being allowed to own two real fights.
- The shipped `Wave 6-8` forge now reads best when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) uses the same three-line hierarchy everywhere: `현재 머신 -> 대표 설치 -> 전투 요청`. Keep `createBaseRouteForgeContextMarkup(...)`, `createBaseRouteForgeHeadlineCardMarkup(...)`, `createBaseRouteForgeCompactCardMarkup(...)`, and [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) aligned on `전투 요청` rather than `proof`/`rider` wording for shipped forge cards. If a future pass brings back `다음 시험`, `rider`, or inline `설치 · ...` strings on the compact Wave 6 forge just to explain pacing, critique will correctly say the reward shell is still selling route administration before the install fantasy.
- The shipped `Wave 6` reward beat now holds cleaner when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) treats the chassis-breakpoint stop as `one doctrine-owned featured install package + one explicit Emergency Vent fallback`, not a three-chassis menu or a second headline weapon card. Keep `buildWave6ChassisBreakpointChoices(...)`, `buildForgeChoices(...)`, and [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) aligned on `featured support install in headline / fallback in rider` for `Wave 6`; if a future pass restores multiple equal-weight chassis cards or a `주포` rider at the same stop, critique will correctly say the support chapter is still being buried under prototype-choice noise instead of one readable install moment.
- The shipped compact route reads closer to a release board when live HUD and forge context both keep `현재 머신 -> 설치/다음 설치 -> 짧은 전투 ask` as the stable hierarchy, even after a real support install lands. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteOwnedPowerSummary(...)` returning machine-first support states and `createBaseRouteForgeContextMarkup(...)` rendering the owned machine as the large line with the picked install only as the secondary line; keep [playables/cinder-circuit/styles.css](/Users/seren/workspace/poong-game/playables/cinder-circuit/styles.css) and [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) aligned on the new `forge-context-spotlight__eyebrow` and on assertions that `Prism Crown/Twin Spine` appears before `Dominion Sweep/Ember Ring`. If a future pass promotes installed support names back to the main live HUD/context headline just to make rewards feel bigger, critique will correctly say the shipped status/forge shell is still selling installs ahead of current loadout readability.
- The shipped `Wave 6-8` support chapter holds better when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) lets `shouldHoldWave6SingleAxisBreakpoint(...)` suppress fresh `Wave 7` installs without suppressing upgrades to the already installed support silhouette. Keep `getVisibleSupportOfferSystemIds(...)`, `shouldOfferSupportSystem(...)`, and [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) aligned on `Wave 7 = installed support upgrade only`, not `no support reward at all` and not `reopen the whole catalog`. If a future pass closes `Wave 7` support again, critique will correctly say the `Wave 6` install still does not own enough of the back half to create replay hunger.
- The shipped `Wave 8` fork now holds better when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) keeps `getVisibleSupportOfferSystemIds(...)`, `scoreBaseRouteSupportHeadlineChoice(...)`, and `buildForgeChoices(...)` aligned on `installed Mk.II payoff -> headline / second support install -> rider / greed -> gamble` for `wave6ChassisBreakpoint` runs. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting doctrine-owned `Wave 8` support forks stay at three visible silhouettes like `Ember Ring + Seeker Array + Aegis Halo`, not a single reopened doctrine card. If a future pass collapses `Wave 8` back to one support id or pulls a weapon card into `rider` just to preserve the old headline/rider rhythm, critique will correctly say the shipped back half still cannot grow into recognizable offense/defense/utility ownership.
- The shipped `Wave 8` reward moment now holds better when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) lets an already installed support system own the compact late-break spotlight instead of letting `Cataclysm Arsenal` / `Warplate Halo` / `Black Ledger Heist` retake the whole headline. Keep `getBaseRouteLateBreakSupportSummary(...)`, `getBaseRouteForgeChoiceTransformation(...)`, `getBaseRouteForgeSpotlightSummary(...)`, `getBaseRouteForgeDominantInstallHero(...)`, and the late-break `BREAK` combat feed aligned on `installed support title -> short Wave 8 ask`, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting a representative Wave 8 card still has its unique card title while spotlighting `Aegis Halo`/other installed support names. If a future pass lets late-break spotlight drift back to doctrine-only titles just to make the finale feel bigger, critique will correctly say the first support chapter still does not own the run's back half.
- The shipped `Wave 8` closure now reads best when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) treats `Cataclysm Arsenal`, `Warplate Halo`, and `Black Ledger Heist` as one dominant object plus one short combat ask, not `완성 시험 -> 짧은 승리 랩` roadmap grammar. Keep `getBaseRouteWave8ClosureCopy(...)`, `getLateBreakRoutePreview(...)`, `getBaseRouteForgeSpotlightSummary(...)`, and the consolidated late-break choice descriptions aligned on `설치 -> 다음 전투`, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting those shipped strings stay free of `proof`, `완성 시험`, and `짧은 승리 랩`. If a future pass restores route-step wording just to explain the back half more explicitly, critique will correctly say the reward/status flow is still selling roadmap doctrine instead of an object the player wants now.
- The shipped `RUN` history only stays honest when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) treats upgrade logs as quarantined presentation data, not a raw event dump. Keep `sanitizeConsolidatedBuildState(...)`, `shouldHideShippingUpgradeEntry(...)`, and `getShippingUpgradePresentationLabel(...)` aligned so `시그니처`, `Late Break`, `Afterburn`, `Wave 9-12`, `Ownership Relay`, `Endform Overdrive`, and similar late/admin strings never reach paused/result-facing pills again; keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting sanitized builds retain only compact run beats like `Wave 3 무기 도약` or `안정화`. If a future pass loosens this just to show more history, critique will correctly say the judged 8-wave slice is still briefing a bigger game through leftover logs.
- The shipped `Wave 3` gun break stays hungry when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) keeps late-ascension `description`, `slotText`, `statusNote`, and the `crownfire_overdrive` choice copy on `starts the lane / leaves a seam` language instead of `support 없이도` or `완성형`. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `Crownsplitter Array`, `Slagburst Drive`, `Voltspine Lattice`, `Anvil Prism`, and `Crownflare Turbine` stay free of those completed-gun phrases. If a future pass restores self-sufficient wording to make the Wave 3 spike sound bigger, critique will correctly say the run is spending the Wave 6 install before the player earns it.
- The shipped base-route forge now holds the `one object, not a contract shell` read better when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) renders the top context through `createBaseRouteForgeContextMarkup(...)` as a neutral `forge-context-spotlight` card inside the normal forge layout, not a separate `forge-overlay__panel--contract` / `forge-context--contract` shell. Keep [playables/cinder-circuit/styles.css](/Users/seren/workspace/poong-game/playables/cinder-circuit/styles.css) and [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) aligned on that neutral class name and on assertions that shipped forge context stays free of `contract-shell`. If a future pass restores a dedicated contract panel/layout just to make the forge feel more authored, critique will correctly say the 8-wave slice is still advertising a larger roadmap shell instead of a direct reward beat.
- The shipped `Tab`/status snapshot reads closer to a release board when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) uses `getBaseRouteStatusBoardSummary(...)` for paused `Tab` and compact era helpers, so those surfaces always resolve to `현재 머신 -> 설치/다음 설치 -> 짧은 전투 ask` instead of flipping back to `설치 -> 현재 머신` or `8-Wave Contract` admin phrasing. Keep `getBaseRoutePauseHeroSummary(...)`, `createBaseRoutePauseSnapshotMarkup(...)`, `getForgeEraPlan(...)`, and `createEraContractPanelMarkup(...)` aligned on that order, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting support states show machine-first ordering plus a short note line. If a future pass restores support-first hero ordering or contract headers for “clarity,” critique will correctly say `Tab` is still reading like route admin instead of a quick machine board.
- The shipped `Wave 6-8` ownership lap now holds better when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) gives the compact-route relay/drift/territory hazards an explicit `ownership_breathing` targeting profile that stops over-valuing the player's projected route, elite path, and drop line. Keep `WAVE_CONFIG`, `buildHazardCandidates(...)`, and `scoreHazardCandidate(...)` aligned so those hazards still pressure flanks and pockets but leave a support-install ownership seam open, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `Wave 6-8` plus representative chassis/support proof configs retain that profile. If a future pass drops these waves back to the default hazard targeting bias, critique will correctly say the game is still testing survival by denying route ownership instead of letting the install breathe.
- The shipped `Wave 6` support contract stays honest when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) only treats `Wave 8` support offers as payoff upgrades if a real support system is already installed, and when `shouldHoldWave6SingleAxisBreakpoint(...)` stops hiding support offers through `Wave 7` for polluted `wave6ChassisBreakpoint` states that still have no install. Keep `createSupportSystemChoices(...)`, `getVisibleSupportOfferSystemIds(...)`, the architecture/bastion draft copy, and [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) aligned on `Wave 6 first install, Wave 8 payoff/upgrade`, not `late-form lock` or `Wave 8 first silhouette`. If a future pass restores the old Wave 8 special-case without checking installed support, critique will correctly say the run is still hiding its second pillar until the finale band.
- The shipped `forge` reward beat stays cleaner when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) treats base-route card copy as a sanitized combat promise instead of letting leftover `proof window / proof lap / support 없이도` wording leak back into the player-facing `Wave 3-8` path. Keep `localizeBaseRouteForgeText(...)`, the `crownfire_overdrive` transformation, and the single-axis `bastion_bay_forge` breakpoint proof on short `다음 전투` language, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting shipped forge transformations stay free of `proof` / `support rider` / `support 없이도`. If a future pass restores those phrases just to explain timing, critique will correctly say the forge is still selling route doctrine and completed-gun fantasy instead of one readable object.
- The shipped base-route `forge` context now holds the strict object-first hierarchy better when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) lets `createBaseRouteForgeContextMarkup(...)` render only `picked object -> 현재 머신 -> short combat ask`, with no extra eyebrow, no `전투 ask` row, and no fallback wave/proof label in the top shell. Keep `getBaseRouteForgeDominantInstallHero(...)` and `renderForgeOverlay()` passing the chosen install/mutation as the large line, the owned machine as the only subline, and the ask as the only note; keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting forge context still shows `Ember Ring`/`Twin Spine` or `Payoff Run`/`Afterglow` while staying free of `forge-ask-shell__eyebrow`, `다음 전투`, and other contract-shell leftovers. If a future pass restores extra header grammar just to explain the same reward, critique will correctly say the forge is still briefing doctrine instead of selling the object.
- The shipped `Wave 8` weapon overclock now holds the `Wave 6 install first, weapon finish second` curve better when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) frames `Cataclysm Arsenal` and its forge/status copy as a seam-finisher layered on top of the installed support silhouette instead of a gun that already dominates `support 없이도`. Keep `getBaseRouteWave8ClosureCopy(...)`, `createLateBreakMutationChoice(...)`, and `getForgeChoiceTransformation(...)` on `Wave 6 설치가 벌려 둔 seam` language, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting shipped `Cataclysm Arsenal` copy stays free of `support 없이도` / `완성형`. If a future pass restores self-sufficient gun wording to shipped `Wave 8` mutation copy, critique will correctly say the run is cashing out completion before the install-owned routing matters.
- The shipped `Wave 3` break now holds better when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) frames architecture picks, early mutation promise/proof copy, and the compact ladder as a first gun-direction lock instead of a completed gun. Keep `createArchitectureDoctrineChoice(...)`, `getForgeChoiceTransformation(...)`, `getShippingLadderSteps(...)`, and the instant architecture combat-feed text on `빈 측면 / 복귀선은 아직 남음 -> Wave 6 첫 지원 설치` language, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting those strings. If a future pass restores `support 없이도`, `완성형`, or equivalent full-kit language to shipped `Wave 3-5` weapon presentation, critique will correctly say the run is front-loading completion again before the first install lands.
- The shipped minimal HUD and paused `Tab` now hold the 8-wave contract better when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) uses `getBaseRouteOwnedPowerSummary(...)` for wave-track, pause hero, and shipped `getLiveSideBetSummary(...)`, so the player sees `current hull/weapon/support + recent gain/current machine + short combat ask` instead of `next install / mastery lap` roadmap copy. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting opening live status stays `빈 선체 / 첫 포지 전`, installed-support states read `설치 -> 현재 머신`, and live notes stay on direct movement asks rather than `Wave 6/8` route reminders. If a future pass restores `다음 설치`, `주력 변이`, or `Wave 8 숙련 랩` as the dominant shipped status copy, critique will correctly say Tab/status are still briefing the run instead of showing what the player already owns.
- The shipped `8-wave` contract stays safer when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) forcibly quarantines late-route runtime state before any shipped forge, victory-lap, or result transition. Keep `quarantineShippedLateRouteState(...)` clearing late build ids (`doctrineCapstoneId`, `lateAscensionId`, `afterburnOverdriveId`, `afterburnDominionId`, cashout/overdrive leftovers), post-capstone counters, and live late-wave payloads before `enterForge()`, `beginBaseRouteVictoryLap()`, and `finishRun()`, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) seeding polluted late state before asserting it gets stripped. If a future pass relies on shipped runtime transitions without this quarantine, critique will correctly say the 8-wave route is still carrying fake late-game scaffolding under the hood.
- The shipped base-route forge context reads cleaner when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) treats the top shell as `picked object -> current machine -> short movement hint`, not `proof window -> rider/admin summary`. Keep `getBaseRouteForgeChoiceCombatAsk(...)`, `getBaseRouteForgeSpotlightSummary(...)`, and `renderForgeOverlay()` using choice-specific combat hints plus `dominantFormSummary.label` as the only secondary line, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting Wave 6 forge context shows `Ember Ring`, `Twin Spine`, and the short ask without falling back to `Dominion Sweep` or `다음 전투` copy. If a future pass restores wave-name/proof text to the dominant forge context line, critique will correctly say the reward beat is still selling route grammar instead of the object.
- The shipped run holds its `Wave 6 first real support install` beat better when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) has no live `preview_support` path, no `Primer` support defs, and no fallback preview support stats before a real install exists. Keep `createSupportSystemChoices(...)`, `createForgeRiderSupportChoice(...)`, `buildForgeChoices(...)`, and `computeSupportSystemStats(...)` on installed-support-only behavior, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `Wave 5` forge choices contain no `system` offer and no pre-install support stats. If a future pass restores any pre-`Wave 6` orbit/drone/halo preview just to make the route feel richer, critique will correctly say the run is cheating its growth curve again.
- The shipped `Wave 3+` and `Wave 6+` status board reads cleaner when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) treats the current reward object as the headline instead of defaulting back to `현재 머신 / 다음 랩` grammar. Keep `getShippingContractSummary(...)`, `getShippingMachinePayoffSummary(...)`, `createShippingLadderMarkup(...)`, `createEraContractPanelMarkup(...)`, and `createBaseRoutePauseSnapshotMarkup(...)` on `무기 변이 -> 다음 설치` after the first break and `설치 -> 주력 변이` after a live support install, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting shipped Wave 6+ summaries surface the install title ahead of the weapon line. If a future pass restores `현재 머신` or `다음 랩` as the dominant shipped labels for live install states, critique will correctly say the run is still teaching route structure instead of selling the toy the player just earned.
- The shipped `Wave 6+` support reward now reads better when the installed support title owns the top slot on both forge context and paused `Tab`, with the current machine demoted to the secondary line and the combat ask left as the only short note underneath. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteForgeDominantInstallHero(...)`, `renderForgeOverlay()`, and `getBaseRoutePauseHeroSummary(...)` using support-first hierarchy for live installs, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `설치` precedes `현재 머신` in paused support states while forge install context stays free of a second labeled `다음 전투` row. If a future pass restores machine-first ordering during live support ownership, critique will correctly say the reward shell is still briefing the player instead of selling the install.
- The shipped `8-wave` route stays honest when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) treats any consolidated `beginWave(...)` request past `Wave 8` as an immediate handoff to the short `Victory Lap`, and when `getBaseRoutePostWaveTransition(...)` stops advertising another forge break for impossible post-`Wave 8` states. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting post-`Wave 8` base-route transitions resolve to `victory_lap`, not `forge`, so future passes cannot quietly reopen `Wave 9+` as a still-live shipped combat path. If a future pass restores a `Wave 9` advance on the compact route, critique will correctly say the game is still balancing two runs at once instead of judging the real 8-wave loop.
- The shipped title contract now holds better when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) treats the lean opener as the only real title route instead of a branch guarded by `CONSOLIDATED_12_WAVE_ROUTE`. Keep `renderTitleLaunchPanel()` on one bare-hull launch shell, keep `createAppState()` starting from that same lean route with no title-selected signature state, and keep the title input handlers free of `Digit1-3` or `data-signature-id` selection paths. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `getLeanStartLaunchSummary(createInitialBuild(...))` is identical across signature ids and stays free of `Wave 6`, `Wave 8`, or `Signature` teaching. If a future pass restores title-side signature branching again, critique will correctly say the shipped 8-wave route is still only a mode beside the old onboarding shell.
- The shipped `forge` and paused `Tab` now read cleaner when the base-route spotlight drops contract tags and labeled `전투 ask` rows, leaving one machine/install name plus one short unlabeled tactical hint. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createBaseRouteForgeContextMarkup(...)`, `createBaseRouteForgeCompactCardMarkup(...)`, `createBaseRouteForgeHeadlineCardMarkup(...)`, and `createBaseRoutePauseSnapshotMarkup(...)` hiding base-route `주력 / 버팀 / 설치` chips and the `전투 ask` label in the top shell, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting those labels stay out while the object name and combat hint remain. If a future pass restores the labels for clarity instead of hierarchy, critique will correctly say the shipped forge/Tab still reads like a contract board instead of a fast machine snapshot.
- The shipped `8-wave` route stays honest when [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) keeps `getSanitizedConsolidatedPresentationBuild(build)` as a player-facing hard quarantine for late-form state, not just visible `Wave 9+` labels. Keep the presentation clone clearing `doctrineCapstoneId`, `catalystCapstoneId`, `cashoutSupportId`, `cashoutFailSoftId`, and `crownfireOverdriveId` after the baseline consolidated cleanup, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) seeding those polluted fields before asserting the sanitized presentation build drops them. If a future pass lets consolidated UI clones retain late capstones or cashout ids again, critique will correctly say the default run still carries a hidden roadmap instead of a closed appetite loop.
- The shipped `Aegis Halo` ownership lap reads cleaner when its `Wave 6-8` proof surge stays on one visible ring and spends the power budget on faster intercepts plus larger seam-clearing pulses instead of spawning an extra halo immediately. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteSupportProofSurge(...)`, `computeSupportSystemStats(build, waveNumber)`, and the Aegis branch inside `resolveWaveConfig(...)` favoring roomier arenas, lower active caps, and stronger pulse/cooldown bonuses for `aegis_halo`; keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting surged Aegis stays at `orbitCount === 1` while intercept cooldown and pulse radius spike above baseline. If a future pass gives Aegis another free ring again without replacing it with equally readable routing value, critique will correctly say the first install is overfilling the screen instead of teaching pocket ownership.
- The shipped `Wave 6` support context reads faster when `forge` and `Tab` collapse the installed support slot to the install name plus one short combat ask instead of restating the whole effect payload. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteSupportInstallCombatAsk(...)`, `getBaseRoutePauseHeroSummary(...)`, `createBaseRoutePauseSnapshotMarkup(...)`, and `getBaseRouteForgeSpotlightSummary(...)` using `설치 + Aegis Halo` style summaries for live support states, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting paused support runs show `설치`, the support title, and the short ask without surfacing `다음 랩` or the full effect string. If a future pass pushes the effect payload back into these top slots, critique will correctly say forge/pause are still briefing the install instead of letting the player return to combat quickly.
- The shipped `Tab` snapshot reads cleaner when `Wave 6+` support runs lock the first slot to `현재 머신` and the second slot to the installed support title, instead of flipping that order or replacing it with a long effect explainer. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRoutePauseHeroSummary(...)` returning `current machine -> 설치 / support title` for installed support states, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting paused `Wave 6-8` support runs show `현재 머신`, `설치`, and the support title without surfacing `다음 랩`. If a future pass promotes effect prose back over the machine silhouette, critique will correctly say `Tab` is still reading like a card explainer instead of a quick status board.
- The shipped `Wave 6-8` ownership lap reads more like a real second mutation when offensive support systems change firing patterns during proof surge instead of only getting hidden rate/damage math. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteSupportProofSurge(...)`, `computeSupportSystemStats(build, waveNumber)`, and `updateSupportSystem(dt)` carrying temporary `shotBurstCount` / `shotBurstSpread` boosts, with `Ember Ring` gaining a short Wave 8 ignition-bolt unlock, `Seeker Array` widening into multi-missile volleys, and `Volt Drones` splitting into forked screens. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting those surge volleys spike above baseline stats without permanently changing the stored tier. If a future pass removes the extra volley behavior without replacing it with another obvious pattern mutation, critique will correctly say the Wave 6 install still buffs numbers more than it changes how the screen plays.
- The shipped `Wave 6-8` lap now reads better when the just-installed support system gets a temporary proof surge instead of staying visually flat until a later tier-up. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteSupportProofSurge(...)`, `computeSupportSystemStats(build, waveNumber)`, and the `beginWave(...)` / `beginBaseRouteVictoryLap()` recompute path so single-bay Wave 6 installs gain one visibly larger silhouette and faster firing/deploy cadence through the ownership lap. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `Seeker Array` and `Kiln Sentry` surge stats spike at `Wave 6-8` while baseline non-surge stats stay lean. If a future pass removes that temporary surge without replacing it with another visible carry mechanic, critique will correctly say the Wave 6 install still does not own the screen long enough to build rerun hunger.
- The shipped forge reward now reads more like a real prize when the base-route headline card promotes the installed object into a dedicated top spotlight block instead of another small preview row. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createBaseRouteForgeSpotlightMarkup(...)` feeding `createBaseRouteForgeHeadlineCardMarkup(...)`, and keep [playables/cinder-circuit/styles.css](/Users/seren/workspace/poong-game/playables/cinder-circuit/styles.css) on the larger `forge-card__spotlight` treatment so `Wave 3` weapon breaks and `Wave 6` support installs show the concrete object first. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting shipped headline cards now include `forge-card__spotlight` and no longer rely on the smaller `forge-card__pivot` preview row. If a future pass drops that spotlight block back into inline metadata, critique will correctly say the forge is still reading like a briefing instead of a power spike.
- The shipped `8-wave` contract now holds better when player-facing proof/act helpers hard-clamp to `DEFAULT_ROUTE_WAVE_COUNT` instead of reading raw late-route wave numbers. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getPlayerFacingWaveNumber(...)` and `getPlayerFacingProofWindowSummary(...)`, with base-route result/forge surfaces using those helpers instead of `getImmediateProofWindowSummary(...)` directly. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting player-facing wave `9+` clamps to `8` and the shipped proof window matches `Wave 8`, not `Refuge Run` or other late-route beats. If a future pass reuses raw `Wave 9-12` proof helpers on title/forge/pause/result again, critique will correctly say the default run is still thinking like a roadmap instead of a contained appetite loop.
- The shipped `Tab` pause hero now lands better when `Wave 6+` installs stop hiding behind `현재 머신 + 다음 랩`. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRoutePauseHeroSummary(...)` promoting the installed support spotlight to the headline slot, with `createBaseRoutePauseSnapshotMarkup(...)` leaving the current machine in the secondary slot and keeping only one short `전투 ask` below it. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting paused `Wave 6-8` support runs now show the support label/effect first and no longer foreground `다음 랩`. If a future pass restores the lap label as the first thing the player reads after installing `Aegis Halo`, `Ember Ring`, `Kiln Sentry`, `Seeker Array`, or `Volt Drones`, critique will correctly say the support install is still not the star of the ownership lap.
- The shipped title panel now works better when it sells only `빈 선체 + 첫 도약` and stops previewing the whole `Wave 3 -> Wave 6 -> Wave 8` ladder before combat starts. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getLeanStartLaunchSummary(...)` returning `현재 선체 / 빈 선체` plus `첫 도약 / Wave 3 무기 방향`, and keep `renderTitleLaunchPanel()` / [playables/cinder-circuit/styles.css](/Users/seren/workspace/poong-game/playables/cinder-circuit/styles.css) on the two-card `title-launch-shell__status-strip` instead of the old four-step proof strip. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the launch summary no longer previews `Wave 6` or `Wave 8` in opening detail. If a future pass restores the full ladder on title again, critique will correctly say the opener is pre-solving the run instead of letting the first weapon break earn the rest.
- The shipped forge reward now lands better when the contract shell collapses to one terse `다음 전투` ask and the dominant card carries the support object fantasy by itself. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createBaseRouteForgeContextMarkup(...)` rendering the new `forge-ask-shell`, `trimForgeCombatAsk(...)` keeping the ask shorter than pause/HUD summaries, and `createBaseRouteForgeHeadlineCardMarkup(...)` omitting redundant `다음 시험` copy so the hero card stays object-first. Keep [playables/cinder-circuit/styles.css](/Users/seren/workspace/poong-game/playables/cinder-circuit/styles.css) on the stronger `forge-card--snap` treatment plus `forge-ask-shell` styling, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting shipped forge context no longer falls back to `machine-payoff` or headline `proof` blocks. If a future pass restores `현재 머신 + 다음 시험` above the cards, critique will correctly say the reward moment is reading like a contract review again.
- The shipped `Wave 6-8` reward read now lands better when the shared payoff shell also carries one short combat ask under the installed machine, instead of stopping at `현재 머신 + 다음 전투` alone. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteCombatAskForWave(...)`, `createBaseRouteForgeContextMarkup(...)`, and `createBaseRoutePauseSnapshotMarkup(...)` feeding a trimmed `전투 ask` line into forge/pause summaries, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting those compact surfaces now include the ask without reintroducing admin or three-slot shells. If a future pass removes that line or lets it sprawl back into route doctrine copy, critique will correctly say the support-install moment is visible but still not immediately testable.
- The shipped `Wave 6` forge now lands better when the visible support install becomes the dominant reward card instead of staying a secondary rider under another weapon thesis. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on recurring base-route contract generation promoting `bastion_bay_forge` with `systemChoice` to `headline` at `Wave 6`, and keep `getBaseRouteForgeChoiceTransformation(...)` / `createBaseRouteForgeHeadlineCardMarkup(...)` naming the installed support object plus one short `다음 전투` ask. If a future pass demotes that package back to `버팀` under a generic `주력` gun card, critique will correctly say the run still sells route roles instead of the second machine mutation.
- The shipped route now reads best when title, HUD/pause, and forge all share the same four-beat spine: `조용한 선체 -> Wave 3 무기 방향 -> Wave 6 지원 설치 -> Wave 8 숙련 랩`. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getShippingLadderSteps(...)`, `getShippingContractSummary(...)`, `getLeanStartLaunchSummary(...)`, `getShippingMachinePayoffSummary(...)`, and `getBaseRouteForgeSpotlightSummary(...)` using that same wording, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the shipped route no longer defaults to `proof`, `contract`, `방호 약속`, or `잠긴 형태 증명` on those compact surfaces. If a future pass lets title/pause/forge drift back to different nouns for the same beat, critique will correctly read the run as a prototype taxonomy again.
- The shipped route now reads best when any leftover base-route contract helper also collapses to the same `현재 머신 + 보이는 효과 + 다음 전투` structure already used by the live HUD/forge card, instead of keeping dormant `Era / Headline Form / Survival Rider / Proof Band` scaffolding alive in exported helpers or tests. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getForgeEraPlan(...)` and `createEraContractPanelMarkup(...)` using the compact machine/effect/ask summary for `CONSOLIDATED_12_WAVE_ROUTE`, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting those helpers no longer emit `Era I`, `Headline Form`, `Survival Rider`, or `Proof Band`. If a future pass restores the old era scaffolding in shipped-route helpers just because they are no longer live-rendered, critique will keep correctly flagging two conflicting reward languages.
- The shipped `Wave 6-8` ownership window now lands better when the arena expands each step and the tax comes from one readable hazard, not from a simultaneous enemy-count spike. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `WAVE_CONFIG` giving `Wave 6-8` the `1720x980 -> 1840x1040 -> 1920x1080` arena climb with `activeCap 17 -> 18 -> 19`, slower hazard cadence, and softer relay/drift/territory tuning. If a future pass pushes `Wave 6-8` back toward `20+` live bodies and sub-10s hazard cadence in the compact route, the first support install will stop reading like 2-3 waves of ownership and slide back into cramped survival homework.
- The shipped `Wave 6-8` arc now works better when the chassis breakpoint installs the doctrine's `midrun` support silhouette, not the doctrine's long-run primary payoff. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `buildWave6ChassisBreakpointChoices(...)` preferring `getDoctrineMidrunSupportSystemId(...)` before `getDoctrinePrimarySupportSystemId(...)`, so `Mirror Hunt` lands `Volt Drones`, `Kiln Bastion` lands `Aegis Halo`, and `Storm Artillery` lands `Ember Ring` inside the shipped route. If a future pass flips this back to the primary system, Wave 6-8 will collapse toward the same missile/turret payoff and the support catalog will stop generating distinct mid-run identities.
- `Storm Artillery` now needs the same encounter specificity already given to `Aegis Halo`, `Kiln Sentry`, `Seeker Array`, and `Volt Drones`. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `applySupportProofEncounterConfig(...)` covering `Ember Ring` with `Ring Cut -> Ring Re-ignite -> Ring Overclock`, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting both the `Ember Ring` install path and those proof labels. If a future pass changes Storm Artillery's shipped support install without updating this proof branch, Wave 7-8 will fall back to generic domination copy again.
- The shipped forge reward beat now reads best when the header stops acting like a three-slot contract strip and uses the same `현재 머신 + 다음 전투` shell as the live HUD, while each base-route card shows one concrete payoff row under the promise. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createBaseRouteForgeContextMarkup(...)` reusing `createCurrentMachinePayoffMarkup(...)`, and keep `createBaseRouteForgeHeadlineCardMarkup(...)` / `createBaseRouteForgeCompactCardMarkup(...)` rendering `createBaseRouteForgePreviewMarkup(...)` so the reward moment names the installed silhouette or support toy directly. If a future pass reintroduces `분기 보상` or drops the preview row from shipped forge cards, the reward screen will drift back toward route admin instead of appetite.
- The shipped `Wave 7-8` payoff beat reads better when installed support systems advertise their concrete silhouette, not a generic module slot. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getSupportSystemSpotlight(...)` covering `Ember Ring`, `Aegis Halo`, `Kiln Sentry`, `Seeker Array`, and `Volt Drones`, and keep `getBaseRouteForgePreviewLabel(...)` preferring that spotlight label for shipped rider cards. If a future pass lets offensive support upgrades fall back to `효과`, `SYSTEM`, or raw English titles again, the Wave 7-8 reward beat will slide back into admin instead of reading like an earned second toy.
- The shipped opener now works better when every pre-`Wave 3` player-facing surface behaves like a true bare-hull start instead of a mini roadmap. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getShippingContractSummary(...)` returning `현재 선체 / 빈 선체` before the first weapon break, and keep `renderTitleLaunchPanel()` using `getLeanStartLaunchSummary(...)` so the title shell only sells `조용한 시작 -> Wave 3 무기 굴절`. If a future pass puts `런 실루엣`, `Wave 6`, or closing-lap copy back onto the opening title/pause/HUD contract, the run will start feeling conceptually complete again before the first bend lands on-screen.
- The shipped forge reward beat now lands better when the base-route header mirrors the actual card being awarded, not the route ladder summary. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteForgeSpotlightSummary(...)` driving `renderForgeOverlay()` for the shipped contract path, so the context card reads as `preview label/value + 다음 시험` (or `형태 고정 + 마무리`) instead of `런 실루엣 + 지금 준비/증명`. If a future pass routes forge context back through `getShippingContractSummary(...)`, the reward moment will slide back into contract-reading before the player even looks at the cards.
- The shipped `Wave 5-8` ladder now works better when `Wave 7-8` stay on repeated domination ownership asks instead of rotating into a brand-new pursuit/reset exam. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `WAVE_CONFIG` reading `Payoff Run -> Crown Breach -> Dominion Sweep -> Dominion Proof` for the compact route, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the Act 2 pressure-family string stays `domination|breach|domination|domination`. If a future pass reintroduces a brand-new Wave 7 or Wave 8 pressure family on the shipped slice, the player will go back to learning a new exam instead of enjoying mastery reps on the earned form.
- The shipped `Wave 6` chassis breakpoint should now read as one body-form install, not a bay-management promise. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `buildWave6ChassisBreakpointChoices(...)` using `getWave6BreakpointInstallSummary(...)` so card descriptions and slot text name the new combat posture or installed payoff, not `Wave 8`, `support bay`, or armory reservation grammar. If a future pass reintroduces `Wave 8 bay/uplink/mastery lap` copy onto those default cards, the run will start feeling pre-authored again before the player even proves the new frame.
- The shipped-facing result/status layer now needs a curated run-history view, not raw `build.upgrades` logs. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getShippingUpgradePresentationLabel(...)` and `getShippingUpgradePresentationLabels(...)` filtering out dormant admin like `Ascension Relay`, `Ownership Relay`, `Chassis Breakpoint`, and similar long-route wrappers before anything hits result/pause-facing UI. If a future pass renders `build.upgrades` directly again, hidden late-route grammar will leak back onto the shipped `8-wave` surfaces immediately.
- The result screen should now summarize the compact run as a handful of visible beats, not a debug log. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `finishRun(...)` using the curated `RUN` mini-pills rather than raw `MOD` pills, so end-of-run readouts sell earned form changes instead of internal branch/admin names. If critique still says result/pause feel like documentation, the next bounded pass should apply the same curation to any remaining combat-feed or Tab history strip instead of adding new progression wrappers.
- The shipped-facing 8-wave contract should now stay on one compact hunger curve: `조용한 시작 -> Wave 3 무기 굴절 -> Wave 6 형태 증명 -> 짧은 마감 랩`. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getShippingContractSummary(...)` and `renderTitleLaunchPanel()` selling `Wave 6` as proof of the locked form, not the first promised support spectacle. If a future pass lets title/pause/forge drift back to `첫 방호·보조 설치` as the main contract, the opener will feel too advanced again.
- The shipped `8-wave` status board should now hard-prefer the compact route promise over both long-route admin and installed-support spotlighting. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getLiveSideBetSummary(...)` using `getShippingContractSummary(...)` plus `getShippingLadderFocus(...)` for default readouts, and only let support-specific labels take over when an actual `supportProof` event is live. If a future pass promotes `Aegis Halo`, `Seeker Array`, or similar installed toys back into the default status headline, the run will start advertising build completeness instead of the compact `Wave 3 -> Wave 6 -> closing lap` ladder again.
- The shipped forge shell now reads better when `forge` labels are stage verbs, not bookkeeping. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getShippingContractSummary(...)` using `지금 도약`, `지금 준비`, `지금 증명`, and `지금 마감` for `phase: "forge"` so reward context still points at one present-tense action. If this falls back to `이번 선택` again, the forge will start reading like a route brief instead of a power install.
- The shipped 8-wave contract now has one shared build-chase summary. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getShippingContractSummary(...)` driving [renderWaveTrack](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js), `createBaseRoutePauseSnapshotMarkup(...)`, `createShippingLadderMarkup(...)`, and the shipped forge shell so default surfaces say `런 실루엣 + 다음 도약/현재 추격/마무리` instead of `현재 형태 + 즉시 위협`. If a future pass reintroduces immediate-threat/admin labels on those shared strips, the title/forge/status contract will drift apart again.
- The shipped lean start now supports a hidden `run-start bias` without reopening title/tutorial text. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createInitialBuild(..., { leanStartBiasDoctrineId })` seeding exactly one doctrine-owned bench core plus `leanStartBiasDoctrineId`, and keep `buildArchitectureDraftChoices(...)` using that hidden bias only to sort/tilt `Wave 3` cards. If a future pass reuses `architectureForecastId` for this again, early HUD/roadmap copy will leak doctrine/admin text before the player earns the pick.
- On the shipped `Wave 1-8` route, `Wave 6` chassis breakpoint cards now bundle one doctrine-primary tier-1 support install immediately instead of holding all support spectacle until `Wave 8`. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `buildWave6ChassisBreakpointChoices(...)` selecting `getDoctrinePrimarySupportSystemId(...)` plus `createSupportSystemTierChoice(...)`, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `bayUnlock + systemChoice` on `Wave 6` rider/chassis cards. If a future pass moves this rider back behind `Wave 7-8`, the shipped run will flatten back into a chassis-only proof stretch.
- On the shipped route, `Wave 8` should now read as a mastery/upgrade lap for the already installed support silhouette, not the first time the toy appears. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getVisibleSupportOfferSystemIds(...)` and `createSupportSystemChoices(...)` preserving the doctrine-primary `Mk.II` upgrade window for breakpoint runs, and keep smoke asserting `Wave 8` rider offers upgrade the same installed system. If `Wave 8` reopens broad fresh installs again, the back half will slip back into catalog admin instead of payoff escalation.
- The current risk is doctrine/support mismatch around odd state mutations or future overcommit variants, because `Wave 6` cards now pre-bake a doctrine-owned support choice. Before widening content again, prefer a consolidation pass that checks special routes still adopt the intended doctrine before the bundled rider is generated or applied.
