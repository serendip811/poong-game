# Improver Session

Purpose: persistent memory for the improvement agent.

Use this file to remember:
- recent implementation patterns that worked well
- systems that are getting overbuilt and need consolidation
- important technical risks or balance risks
- short next-step heuristics for bounded, high-value improvements

Keep this short and replace stale notes instead of endlessly appending.

## Current Memory

- On the shipped `Wave 1-8` route, late reward/result copy should now fail closed to the installed object that exists right now, not reserved bays or future brackets. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createForgePreviewRows(...)`, `getBaseRouteForgeChoiceTransformation(...)`, the `bastion_bay_forge` instant draft feed inside `handleForgeSelection(...)`, and `getBaseRouteResultCopy(...)` naming only the chassis/support toy plus the immediate proof window; do not let `빈 보조칸`, `후속 보조 선택`, `Wave 8 uplink`, or `Wave 9` language back onto those default beats. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the cleaned preview rows and result copy. If a future pass reintroduces bay reservation or future-route narration on reward/result surfaces, the late support payoff will slide back into route admin instead of an immediate rerun hook.
- On the shipped `Wave 1-8` route, the earned support rider should now surface inside the same two-slot `현재 형태 + 즉시 위협/증명` shell instead of reopening a third payoff lane or shard admin. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteInstalledRiderSummary(...)` and `getDominantFormSummary(...)` appending the installed rider title (for example `Seeker Array Mk.II`) directly to the dominant-form label once support is actually live, and keep `getLiveSideBetSummary(...)` refusing to surface doctrine pursuit/shard counters on the consolidated route. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting that shipped ladder markup includes the installed rider title while consolidated pursuit status stays `null`. If a future pass moves the rider back into a separate status lane or lets shard progress reappear on default status surfaces, the Wave 7-8 payoff will slide back into route administration instead of a visible run-closing toy.
- On the shipped `Wave 1-8` route, the late support payoff must now fail closed to the `Wave 7 clear -> Wave 8 forge` window only when the run actually passed the lean `Wave 6` single-axis chassis lock. Keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getVisibleSupportOfferSystemIds(...)`, `createSupportSystemChoices(...)`, and the strict `Wave 8` rider contract using `build.wave6ChassisBreakpoint` so ordinary `Wave 8` forges do not reopen random support installs, while true chassis-lock runs get one doctrine-owned `Mk.II` spectacle card (`Seeker Array`, `Kiln Sentry`, `Aegis Halo`, etc.) before the last proof. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting that `Wave 8` locked-form riders surface as `systemTier = 2` support payoffs and that doctrine visibility flips to the doctrine's primary support silhouette at `Wave 8`, not the old midrun fallback. If a future pass lets this support payoff appear on non-breakpoint runs or drops back to plain tier-1 installs, the late appetite beat will flatten back into generic maintenance instead of an earned spectacle spike.
- On the shipped `Wave 1-8` route, `Tab`/pause must now fail closed to the same double-slot strip as the live HUD and forge context: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createBaseRoutePauseSnapshotMarkup(...)` rendering `createBaseRouteStatusStripMarkup(...)` directly instead of the older focus card with `summary-head`, wave chip, and oversized title. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `route-contract--double` is present and `summary-head`/`W5` stay absent from the shipped pause snapshot. If a future pass restores the extra hero header or wave chip on `Tab`, the pause check will slip back toward a document card instead of the intended quick status board.
- On the shipped `Wave 1-8` route, pause/status readouts must now fail closed to the same two-idea contract as forge/HUD: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createBaseRoutePauseSnapshotMarkup(...)` rendering only the hero `현재 형태 + 즉시 위협` block with no `최근 획득` lane, and keep `getLiveSideBetSummary(...)` silent for installed-support spotlights and undeployed cache/ascension teasers on the consolidated path so only active proof labels or already-live pickup/branch states surface. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `pause-summary__lanes` stays absent, the passive `방호 고리` spotlight stays hidden, and live cache summaries appear only after deployment. If a future pass restores recent-gain cards or pre-trigger cache copy to the default status layer, the shipped run will slide back into route administration during pause checks.
- On the shipped `Wave 1-8` route, player-facing roadmap/status/forge strips must now fail closed to two slots in `form first -> immediate proof ask second` order: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createBaseRouteStatusStripMarkup(...)` supporting `headlineFirst`, keep `createRoadmapMarkup(...)`, `createShippingLadderMarkup(...)`, `createBaseRouteForgeContextMarkup(...)`, and `renderWaveTrack()` passing only `현재 형태` plus `즉시 증명/즉시 위협`, and keep the shipped roadmap text free of `Wave 8 마무리 포지`, `분기 보상`, or reserved support/bay promises. Keep [docs/games/cinder-circuit-design.md](/Users/seren/workspace/poong-game/docs/games/cinder-circuit-design.md) aligned on `Wave 5 weapon leap -> Wave 6 chassis lock -> Wave 7-8 proof`, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the double-slot markup and the absence of `Wave 8` support wording in shipped ladder/roadmap text. If a future pass restores a third slot or reintroduces `Wave 6 support lock / Wave 8 bay` language on shipped surfaces, the breakpoint will slip back into prototype route-briefing instead of a clean earned form spike.
- On the shipped `Wave 1-8` route, the `Wave 6` chassis breakpoint must now fail closed across doctrine copy, forge transformation copy, and forge context: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on doctrine `supportDoctrineText` reopening only at `Wave 8`, keep `getBaseRouteForgeChoiceTransformation(...)` from ever resurfacing `body/support bracket`, immediate install, or `Wave 8 마무리 포지` bundle language for `bastion_bay_forge`, and keep `getBaseRouteForgeContextTailSummary(...)` returning `null` so the forge shell stays on installed chassis plus one immediate proof ask only. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the empty forge tail and the single-axis transformation copy. If a future pass reopens a third forge-context slot or lets doctrine/utility text promise a bundled Wave 6 support/bay payoff again, the breakpoint will slide back into prototype package fantasy instead of an earned chassis lock.
- On the shipped `Wave 1-8` route, `Wave 6` chassis picks must now fail closed to a true single-axis breakpoint again: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `buildWave6ChassisBreakpointChoices(...)` returning chassis-only `singleAxisBreakpoint` cards with no `bayUnlock` or `systemChoice`, and keep `applyForgeChoice(...)` writing `Chassis Breakpoint: support hold through Wave 7 proof laps` while leaving support capacity/install unchanged until the `Wave 8` reopen. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `Wave 6 = chassis only`, `Wave 7 = no visible support offer and no gamble lane`, and doctrine-owned support visibility reopening at `Wave 8`. If a future pass quietly reattaches a live install or bay unlock to those cards, the shipped breakpoint will slide back into package administration before the body leap is memorable on its own.
- On the shipped `Wave 6-8` support proof ladder, `Volt Drones` must now fail closed to a real three-step rear-screen script instead of the shared `Claim Space` loop: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `applySupportProofEncounterConfig(...)` extending through `Wave 8` for `volt_drones`, with `Drone Screen` / `Drone Re-entry` / `Drone Overclock` swapping in shrike-brander chase cuts, binder tail-seam re-entry pressure, and a Wave 8 rolling-refuge payoff around brute/binder rear deletion. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting all three labels plus the live overclock status. If a future pass removes the drone-specific support-proof branch or lets those waves fall back to generic lane-ownership wording again, the drone route will slide back into generic support art instead of a rerunnable rear-screen build.
- On the shipped `Wave 6-8` support proof ladder, `Seeker Array` must now fail closed to a real three-step missile-routing script instead of the shared `Claim Space` loop: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `applySupportProofEncounterConfig(...)` extending through `Wave 8` for `seeker_array`, with `Seeker Sweep` / `Seeker Shear` / `Seeker Overclock` swapping in outer-pocket skimmer/mortar cleanup, repeated same-flank crosslane cuts, and a Wave 8 barrage-corridor payoff around warden/mortar backline deletion. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting all three labels plus the live overclock status. If a future pass bypasses the seeker-specific overlay or lets those waves fall back to generic lane-ownership wording again, the missile lane will slide back into generic support art instead of a rerunnable outer-lane build.
- On the shipped `Wave 6-8` support proof ladder, `Aegis Halo` must now fail closed to a real three-step proof script instead of the shared `Claim Space` loop: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `applySupportProofEncounterConfig(...)` extending through `Wave 8` for `aegis_halo`, with `Halo Scissor` / `Halo Re-entry` / `Halo Bastion` swapping in shooter-heavy mixes, faster relay-drift-pocket timing, and directives that explicitly teach bullet-cutting plus tight re-entry. Keep `getLiveSideBetSummary(...)` prioritizing `wave.supportProof` above the generic installed-support spotlight so those proof labels actually surface during combat, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting all three labels plus the overclock pocket-hold readout. If a future pass narrows the guard back to `Wave 6-7`, or lets the live HUD show only `방호 고리` while the proof is active, Halo will slide back into a generic shield install instead of a rerunnable midrun combat identity.
- On the shipped `Wave 1-8` route, `Kiln Sentry` must now fail closed to a real `Wave 6-7` proof ask, not the shared relay/drift script: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `applySupportProofEncounterConfig(...)` wrapping `resolveWaveConfig(...)` after the chassis overlay, and keep `kiln_sentry` installing `Kiln Crosshold` / `Kiln Reclaim` with extra `binder/warden/brander` pressure, slower relay/drift tempo, and `forward pocket hold` / `reclaim pocket loop` live summaries. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting both support-proof labels plus the updated shipped ladder/roadmap text that now says support installs with the Wave 6 chassis lock. If a future pass bypasses the support-proof overlay or lets the ladder fall back to `Wave 7-8 rider later` wording, the sentry lane will go back to feeling like generic add-on fire instead of a rerunnable posture change.
- On the shipped `Wave 1-8` route, `Wave 6` chassis breakpoint picks must now fail closed to `차체 + 즉시 support install`, not `차체만`: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `buildWave6ChassisBreakpointChoices(...)` attaching one doctrine-preferred `systemChoice` plus `bayUnlock` to every consolidated chassis card, and keep `applyForgeChoice(...)` still triggering the chassis surge while logging `support online now, auto Wave 8 uplink`. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting Wave 6 rider cards now carry live installs, applied builds jump to `supportBayCap = 2` with one installed system, broad new support offers stay hidden at `Wave 7`, and the `Wave 7` field grant can still surface the installed system's tier-up proof. If a future pass removes the immediate install, forgets the chassis surge on `bayUnlock` cards, or closes the follow-up tier-up window again, the non-gun branch will collapse back into a finale tease instead of a multi-fight identity.
- On the shipped `Wave 5-8` rider path, installed support rewards must now fail closed to one shared payoff helper instead of per-surface fallback nouns: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getSupportSystemInstalledPayoff(...)` feeding the rider-step forge tail and the instant draft feed whenever `Aegis Halo` or another real support install resolves, so those beats reuse the same `방호 고리 / 탄막 절개 + 방호 파동` contract the live HUD already shows. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the helper plus the rider-tail payload. If a future pass lets instant support installs fall back to `support bay`, `uplink`, or raw system titles again, the non-offense payoff will slide back into route administration during the exact pick-confirm beat that should feel appetizing.
- On the shipped `Wave 5-8` rider path, `Aegis Halo` must now fail closed across every live surface as one installed shield identity, not a support-admin lane: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getSupportSystemSpotlight(...)` driving the `Aegis Halo` card promise/proof, the active `getBaseRouteBranchPayoffSummary(...)` strip, `getLiveSideBetSummary(...)`, and the resolved forge feed so all of them say `방호 고리` / `탄막 절개 + 방호 파동` or the same present-tense shield read instead of `분기 보상`, bay/uplink, or shard-route text. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the spotlight helper plus an installed `Aegis Halo` build's payoff/status outputs. If a future pass lets the live strip or feed fall back to generic support labels again, the defense lane will go back to feeling like route maintenance instead of a rerunnable shield fantasy.
- On the shipped `Wave 5-8` rider path, `Aegis Halo` should now fail closed as an immediate defense identity instead of a future support promise: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `SUPPORT_SYSTEM_DEFS.aegis_halo` carrying an intercept pulse from tier 1, keep `updateSupportSystem(...)` routing that pulse through `triggerChassisPulse(...)` with enemy-shot clearing enabled, and keep `createForgePreviewRows(...)` plus `renderForgeOverlay()` using `getSupportSystemIdentitySummary(...)` so the card/contract reads `방호 고리` and `탄막 절개 + 방호 파동` instead of `다음 보조`. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting both the pulse-enabled stats and the new preview labels. If a future pass removes the tier-1 pulse or lets the rider copy fall back to generic support/admin wording, the defense lane will slip back into paperwork instead of a rerunnable shield fantasy.
- On the shipped `Wave 1-8` route, any late-break helper that survives for tooling or fallback state must now fail closed to `Wave 8 완성 시험 -> 짧은 승리 랩`: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteWave8ClosureCopy(...)` feeding `getLateBreakCadenceSummary(...)`, `getLateBreakHeadline(...)`, `getLateBreakRoutePreview(...)`, consolidated late-break card `description/roadmapDetail`, late-break forge transformation proof copy, and late-break status notes. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting those shipped late-break cards and preview rows no longer mention `Wave 9-12`. If a future pass lets `Cataclysm Arsenal`, `Warplate Halo`, or `Black Ledger Heist` promise `Wave 9` or `Final Stand` again on the shipped path, the rerunnable route will start reading like a teaser bracket instead of a closed Wave 8 payoff loop.
- On the shipped `Wave 1-8` route, `Tab`/pause must now fail closed even harder to `현재 형태 + 즉시 위협 + 최근 획득` only: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createBaseRoutePauseSnapshotMarkup(...)` rendering exactly one lower card `최근 획득` and never reopening `활성 보조` or `활성 판돈` rows on the default pause surface, even when support or greed is live. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting that greed and chassis-proof pause snapshots still show the earned value but no longer surface those lane labels. If a future pass reintroduces support/greed lane callouts under pause because a branch is active, the shipped `Tab` board will drift back toward route taxonomy instead of the one-headline quick board the latest critique asked for.
- On the shipped `Wave 1-8` route, Wave 6 doctrine adoption must now fail closed against pursuit/admin scaffolding: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `applyForgeChoice(...)` skipping `autoCommitDoctrinePursuit(...)` for consolidated `wave6_ascension`, keep `sanitizeConsolidatedBuildState(...)` clearing stale `doctrineChaseClaimed / doctrinePursuit* / overcommit*` flags plus upgrade-log entries like `교리 추격`, `Forge Pursuit`, `Contraband Overcommit`, and `Ascension Relay`, and keep shipped doctrine/support descriptions from reintroducing `marked elite shard` or `supportDoctrineText` admin copy. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting that fallback `Wave 6 Ascension` leaves pursuit off and that a polluted shipped build gets those fields and upgrade strings scrubbed. If a future pass lets Wave 6 silently arm pursuit again, the midrun proof laps will slide back into shard/admin homework instead of one form proving itself twice.
- On the shipped `Wave 1-8` route, the base forge header must now fail closed to `즉시 위협 + 현재 형태` unless the player is literally on the rider step: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `renderForgeOverlay()` routing its contract tail through `getBaseRouteForgeContextTailSummary(...)`, and keep that helper returning `null` unless `riderStep` is live with a concrete `branchPreviewPayoff`. This preserves the compact `Hades`/`Nova Drift`-style reward read for headline and final picks while still allowing `새 보조` or equivalent to appear exactly when the player is choosing it. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the helper returns no tail off-rider and returns the preview label/value on rider. If a future pass lets active support/greed/chassis state repopulate the forge header outside the live rider pick, the shipped reward pause will drift back toward branch administration instead of immediate appetite.
- On the shipped `Wave 1-8` route, `Wave 6` chassis breakpoint choices must now stay single-axis through the next proof lap: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `buildWave6ChassisBreakpointChoices(...)` returning chassis-only `singleAxisBreakpoint` cards with no immediate `bayUnlock` or `systemChoice`, keep `shouldHoldWave6SingleAxisBreakpoint(...)` delaying both support offers and the secondary gamble lane through `Wave 7`, and keep `applyForgeChoice(...)` marking that breakpoint with `wave6ChassisBreakpoint = true` plus the `support hold through Wave 7 proof laps` upgrade note. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting that `Wave 6` rider cards are chassis-only, `Wave 7` stays a two-card no-system/no-gamble stop after that pick, and support visibility reopens at `Wave 8`. If a future pass quietly reattaches `systemChoice`, `bayUnlock`, or a `Wave 7` gamble card to the shipped breakpoint, the run will slide back into package administration before the chassis proof is memorable on its own.
- On the shipped `Wave 1-8` route, the opener must now stay on a one-bay support contract until the player earns a real chassis break: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `BASE_BUILD.supportBayCap = 1`, `getSupportBayCapacity(...)` using the shipped `1 -> 2 -> 3` curve, `applyAuxiliaryJunction(...)` only opening the second bay at `Wave 6`, and `unlockLateSupportBay(...)` capping the shipped route at three bays instead of inheriting the old `2 -> 3 -> 4` ladder. Keep the consolidated `Wave 6` chassis/break feed strings on `두 번째 support bay now, 세 번째 bay at Wave 8`, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the new base cap plus the updated `Wave 6` / `Wave 8` milestones. If a future pass quietly restores `supportBayCap: 2` at run start, the opener will start reading preloaded again before the first real transformation lands.
- On the shipped `Wave 1-8` route, late-route contamination must now fail closed through sanitized presentation copies, not by mutating every helper callsite: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `sanitizeConsolidatedBuildState(...)` clearing preview support, wildcard, `lateBreak`, `lateAscension`, `Afterburn`, illegal overclock, and late-field mutation state; keep `getSanitizedConsolidatedBuild(...)` feeding shipped surfaces like `buildForgeChoices(...)`, `getBuildRoadmap(...)`, `getForgeEraPlan(...)`, `createBaseRoutePauseSnapshotMarkup(...)`, `getSupportSystemDef(...)`, and `computeSupportSystemStats(...)`; and keep `refreshDerivedStats(...)` scrubbing the live `state.build` before combat HUD/runtime stats update. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting that a polluted shipped build cannot re-surface `preview_support`, `wildcard_protocol`, `late_ascension`, `afterburn_overdrive`, or `afterburn_dominion`, and that shipped roadmap/pause markup stay free of `Wave 9`, `Afterburn`, and `Cataclysm`. If a future pass makes another player-facing helper read raw build late-route fields directly, the shipped run will start feeling like a teaser for a different route again.
- On the shipped `Wave 1-8` route, support/chassis reward text must now fail closed to the live contract and never sell `Late Break Armory`, `Wave 9-12`, or `Afterburn` as the payoff for current branch cards: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on the doctrine `supportDoctrineText`, `SUPPORT_SYSTEM_DEFS.seeker_array.tiers[1].description`, and `createBastionSystemsForgeChoice(...)` all speaking in `Wave 6 support online -> Wave 8 마무리 포지` language instead of late-route admin. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting that the shipped Seeker Array text and `Wave 6` chassis+support choice descriptions no longer mention `Late Break Armory` or `Wave 9`. If a future pass reintroduces those late nouns into live support cards, the shipped run will start reading like onboarding for a different game again.
- On the shipped `Wave 1-8` route, `Wave 6` is now the real support unlock and the forge shell must sell it as `즉시 위협 / 현재 형태 / 새 보조`, not `body first, support later` or `headline pick + next proof`: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `SUPPORT_SYSTEM_START_WAVE = 6`, keep `getVisibleSupportOfferSystemIds(...)` narrowing `Wave 6-8` to one doctrine-owned support silhouette instead of reopening the whole catalog, and keep the base-route branch of `renderForgeOverlay()` feeding `createBaseRouteForgeContextMarkup(...)` with `proofWindow.label`, `dominantFormSummary.label`, and the live/pending support payoff when the rider step is open. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `Wave 5` has no real support offer, `Wave 6` does, and the shipped support defs all unlock at `6`. If a future pass moves support back to `Wave 7` or restores `다음 시험` as the dominant base-route forge header, the run will drift back toward late documentation instead of an earned midrun support appetite.
- On the shipped `Wave 1-8` route, `Tab`/pause must now read as `what am I / what kills me / what did I just earn`, not a route taxonomy board: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createBaseRoutePauseSnapshotMarkup(...)` using the hero card as `현재 형태 + 즉시 위협`, always appending one `최근 획득` card from the latest player-facing `build.upgrades` entry, and only appending side cards `활성 보조` / `활성 판돈` when those lanes are actually live. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting that opening pause shows `최근 획득: 첫 포지 전`, greed pause shows `활성 판돈`, support pause shows `활성 보조`, and placeholder `Bare Hull` / `조용한 계약` text stays hidden. If a future pass puts `다음 시험`, fixed route labels, or empty lane taxonomy back on the default pause surface, the run will drift back away from the `Hades`/`Brotato` quick-status contract.
- On the shipped `Wave 5-8` route, `Scrapline Raid` must now read as a staged greed chassis fantasy, not a static economy modifier: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `applyMidrunGreedRouteConfig(...)` naming the active raid phase per wave (`Entry Vault -> Tow Fork -> Caravan Hook -> Jackpot Fork`), keep `getBaseRouteBranchPayoffSummary(...)` surfacing that live raid phase instead of the generic contract name, and keep `fireMidrunGreedRaidVolley(...)` plus `drawPlayerChassisFrame(...)` scaling the tow-fork silhouette and extra center harpoon as the route moves through `Wave 6-8`. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the staged route labels and branch-payoff readout. If a future pass collapses the greed lane back to one static `Scrapline Raid` label or one unchanged twin-fork volley across all three proof laps, ledger runs will slide back toward accounting flavor instead of a rerunnable combat identity.
- On the shipped `Wave 1-8` route, `Wave 6` chassis choices must now prove a real defense/support lane through encounter shape, not just hidden stats: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `resolveWaveConfig(...)` flowing through `applyChassisBreakpointEncounterConfig(...)` so non-greed `Wave 6-7` runs get chassis-owned arena/mix/hazard overlays plus a short `chassisProof` cue in `beginWave()` / `getLiveSideBetSummary()`. `Vector Thrusters` should keep the far-relay / double-pocket dash proof, `Bulwark Treads` should keep the anchor-corridor / refuge-pocket hold proof, and `Salvage Winch` should keep the drop-line / surge-reset loop. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting those three overlays and that `midrunGreedRouteUntilWave` suppresses the chassis proof so greed still owns its own route. If a future pass removes these encounter overlays or lets greed/support collapse back into the same `Breakline Relay -> Crownfire Drift` read, the `Wave 6` lane choice will go back to feeling like body bookkeeping instead of a rerun-worthy build commitment.
- On the shipped `Wave 1-8` route, paused combat must still fail closed to the minimal HUD shell: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `shouldUseMinimalBaseRouteHud(...)` staying minimal during paused combat, keep `renderHudPanels()`, `renderCombatFeed()`, and `shouldShowLiveBranchPayoff(...)` from reopening the multi-row feed or live `분기 보상` strip just because the player paused, and keep `renderPauseOverlay()` filling `#pause-summary` via `createBaseRoutePauseSnapshotMarkup(...)` without roadmap rows, future-proof copy, or branch labels that are not yet live. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the minimal-HUD behavior alongside the new `즉시 위협 / 최근 획득` snapshot contract. If a future pass lets pause re-expand feed/history or reintroduces future-route wording there, `Tab` will drift back into a document instead of a glance board.
- On the shipped `Wave 1-8` route, live combat/reward messaging must now fail closed to `one headline + one proof cue`, not stacked admin text: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `renderCombatFeed()` showing only the newest summarized feed line during active play, keep `renderWaveTrack()` hiding `분기 보상` unless the player is paused or in result state, and keep the base-route branch of `renderForgeOverlay()` using `createBaseRouteForgeContextMarkup(...)` as `headline pick + next proof` instead of `current form + next ask + branch payoff`. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting both the custom forge `다음 시험` strip and `summarizeCombatFeedEntry(...)` sentence split. If a future pass restores a persistent four-row feed or reintroduces live branch/support/admin rows before pause, the run will drift back into prototype narration instead of immediate appetite.
- On the shipped `Wave 1-8` route, the first midrun forge must now fail closed to `Wave 5 headline + subordinate survival fallback`, not a three-appetite branch: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `shouldOpenBaseRouteSecondaryBranch(nextWave)` delaying the extra `판돈` lane until `Wave 6`, keep the `Wave 4 -> Wave 5` forge feed explicitly saying the greed split returns only after the `Wave 5` proof lap, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `Wave 5` forge choices resolve to only `headline|rider`. If a future pass reopens `field_greed` or a third contract tile at `Wave 5`, the first real payoff will slide back into build bookkeeping before the new gun form has owned even one room.
- On the shipped `Wave 1-8` route, the `Wave 5-8` midrun exam must now fail closed to `domination -> breach -> pursuit -> pocket hold`, not four straight open-lane ownership laps: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `Wave 6` using the `breach` cell plus `Breakline Relay`, `Wave 7` using the `pursuit` cell plus `Crownfire Drift`, and `Wave 8` using the new `reset` hold cell plus `Forgecross Pocket`; keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the `none|relay|drift|territory` hazard cadence and `domination|breach|pursuit|hold` pressure-family ladder across Act 2. If a future pass flattens `Wave 6-8` back into hazardless domination laps, the run will again feel solved before the first gun/body branch has proved itself through different movement asks.
- On the shipped `Wave 1-8` route, future-hardware teaser state must now fail closed completely: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `sanitizeConsolidatedBuildState(...)` clearing both `previewSupportSystemId` and `wildcardProtocolIds`, keep `createPreviewSupportChoice(...)` / `createForgeRiderSupportChoice(...)` from even considering primer or contraband cards under consolidation, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting that polluted shipped builds do not regain preview support stats or wildcard picks. If a future pass lets stale primer/wildcard state leak back into support summaries, rider cards, or `Wave 5` forge choices, the run will drift back toward fake breadth before the gun/body spine has finished owning the screen.
- On the shipped `Wave 1-8` route, `Wave 6` chassis breakpoint cards must now fail closed to `body first, no installed support`, not `chassis + rider bundle`: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `buildWave6ChassisBreakpointChoices(...)` returning chassis-only `bastion_bay_forge` picks for the consolidated route, keep `applyForgeChoice(...)` relying on the existing `!choice.bayUnlock && CONSOLIDATED_12_WAVE_ROUTE` chassis-surge path instead of `applyAuxiliaryJunction(...)` plus `systemChoice`, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting `Wave 6` leaves `supportBayCap` at `2` with no installed systems. If a future pass re-bundles a tier-1 support install into `Wave 6`, the midrun branch will go back to reading like layered package admin instead of one rerun-worthy movement/body memory.
- On the shipped `Wave 1-8` route, status/forge contract surfaces must now fail closed to `현재 전장 / 현재 형태 / 분기 보상` instead of ladder paragraphs or future-lane copy: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteBranchPayoffSummary(...)` surfacing only a live greed raid, installed support/chassis payoff, or nothing; keep `renderWaveTrack()` and `createBaseRouteForgeContextMarkup()` feeding that third slot only when a real branch is active; and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting both the `분기 보상` strip and the absence of fallback roadmap/future labels. If a future pass restores `런 실루엣`, roadmap step paragraphs, or generic `다음 급등` copy on shipped HUD/forge surfaces, the live run will drift back into reading about route structure instead of one-glance combat ownership.
- The shipped docs must now fail closed to the honest `8웨이브 + 짧은 승리 랩` slice, not `12웨이브 / 세 시대`: keep [docs/games/cinder-circuit-design.md](/Users/seren/workspace/poong-game/docs/games/cinder-circuit-design.md) and [docs/games/cinder-circuit-source-analysis.md](/Users/seren/workspace/poong-game/docs/games/cinder-circuit-source-analysis.md) aligned to the live route whenever player-facing contract language is touched. If those docs drift back to `Wave 9-12` or `세 시대` as the default shipped promise, later UI work will keep regrowing absent payoff framing.
- On the shipped `Wave 1-8` route, the `Wave 5` `Scrapline Raid` branch must now fail closed to immediate `Wave 5-8` arena takeover, not `Wave 6` delayed payoff: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `isMidrunGreedRaidFrameActive(...)` opening at `resolvedWave >= 5`, keep `applyMidrunGreedRouteConfig(...)` injecting the `Wave 5` `Scrapline Entry Vault` pocket before the existing `Wave 6-8` salvage/caravan beats, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting both the `Wave 5` active raid frame and the new `entry vault pocket` config. If a future pass slides this branch back to `Wave 6`, the first honest `판돈` fork will return to reading like a promise instead of a run-shaping combat bet.
- On the shipped `Wave 1-8` route, the first real `판돈` branch must now fail closed to a guaranteed `Wave 5 Scrapline Raid`, not a utility reroll: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `buildForgeChoices(...)` hard-preferring `field_greed` for the `nextWave === 5` gamble slot, keep `scoreBaseRouteGambleChoice(...)` heavily biasing that same opening contract toward greed, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting the `Wave 5` gamble card is specifically `Scrapline Raid`. If a future pass lets `recycle`, `magnet_rig`, or other tidy utility options retake that first `판돈` slot, the shipped route will drift back into reading about branch depth instead of making the player pilot one by `Wave 6`.
- On the shipped `Wave 1-8` route, the `Wave 5` greed branch must now fail closed to a real `Wave 6-8` raid frame, not just hazard rerouting: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `isMidrunGreedRaidFrameActive(...)` / `shouldUseSalvageSurgeKit(...)` driving pickup pull, salvage surge, and tow-fork volley hooks whenever the greed route is live, keep `beginWave()` / `applyForgeChoice(... field_greed ...)` seeding the burst window so the route starts visibly hot, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting both the active `Wave 6-8` window and the seeded `chassisSalvageBurstTime`. If a future pass keeps the salvage/caravan hazards but drops these runtime hooks, `판돈` will drift back to reading like economy math instead of a branch the player can actually pilot.
- On the shipped `Wave 1-8` route, the `Wave 5` greed branch must now fail closed to a persistent `Wave 6-8` cash-out route, not just scrap math: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `createFieldGreedContractChoice(...)` stamping `midrunGreedRouteUntilWave = DEFAULT_ROUTE_WAVE_COUNT`, keep `applyMidrunGreedRouteConfig(...)` injecting `salvage -> caravan -> salvage` hazards across `Wave 6-8`, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting those three hazard beats after applying the midrun greed choice. If a future pass removes that route state or lets `Greed Contract` fall back to only scrap/pickup bonuses, the shipped `판돈` lane will collapse back into admin economy instead of changing movement and pickup behavior for multiple combats.
- On the shipped `Wave 1-8` route, `Wave 6` must now fail closed to `chassis lock + one doctrine-filtered tier-1 support install`, not chassis-only delay: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `buildWave6ChassisBreakpointChoices(...)` using the doctrine-filtered midrun support pool even though `SUPPORT_SYSTEM_START_WAVE === 7`, keep the recurring `rider` slot on `bastion_bay_forge` with `bayUnlock: true` plus a bundled `systemChoice`, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting a real `Wave 6` bundled install. If a future pass reverts `Wave 6` back to body-only lockout, the shipped route will collapse back into teasing non-gun identity too late to earn two proof laps.
- On the shipped `Wave 1-8` route, shared roadmap and ladder text must now describe `Wave 6 body/support bracket -> Wave 7-8 proof`, not `Wave 6 body only -> Wave 8 first rider`: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on the updated `getBaseRouteBuildRoadmap(...)`, `getShippingLadderSteps(...)`, and `getBaseRouteForgeChoiceTransformation(...)` wording so player-facing summaries consistently sell the bundled midrun support proof window. If a future pass restores `Wave 8 first support rider` text while the live draft still installs one at `Wave 6`, HUD/forge/roadmap will disagree again about where the non-gun branch actually starts.
- On the shipped `Wave 1-8` route, title/HUD/forge contract surfaces must now fail closed to `current battlefield + current form` only until the domination window is proven: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteTransformationFocus(..., { stage: "title" }) = 빈 선체 돌입`, keep `createBaseRouteStatusStripMarkup()` defaulting to a two-slot `route-contract--double` shell unless a third slot is explicitly needed elsewhere, and keep `createBaseRouteForgeContextMarkup()` / `renderWaveTrack()` / base-route forge context free of `다음 급등` or any other next-break teaser. If a future pass restores Wave 3 promise text on the title or a third future-facing slot on the live strip, the shipped run will drift back into pre-spending anticipation instead of letting the first break earn its own hunger.
- On the shipped `Wave 1-8` route, the live bottom-left HUD panel must now fail closed to a neutral status board, not a roadmap card: keep [playables/cinder-circuit/index.html](/Users/seren/workspace/poong-game/playables/cinder-circuit/index.html) mounting `status-board status-board--overlay` around `run-track-label` and `wave-track`, keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) stamping that panel with short combat labels only (`Wn`, `FORGE Wn`, `LAP`, `RUN CLEAR`), and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) rejecting the old `roadmap-card roadmap-card--contract` markup in the shipped HUD. If a future pass restores a roadmap-branded panel or sentence-length wave labels during combat, the run will drift back toward route reading instead of one-glance pressure/form tracking.
- On the consolidated shipped `Wave 1-8` route, shared roadmap helpers must now fail closed to the same three live promises `Wave 3 first weapon break -> Wave 6 chassis lock -> Wave 8 proof/lap`; keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteBuildRoadmap(...)` driving `getBuildRoadmap(...)` during the consolidated path so `getDominantFormSummary(...)`, `getNextBreakpointSummary(...)`, forge focus, and any reused summary card stop falling back to `midform/capstone/12-wave` staging. If a future pass restores `Monster Form Lock`, `Midform Spike`, or other generic late-route rung names on the base route, title/HUD/forge will start disagreeing about where the real payoff lives again.
- On the shipped `Wave 1-8` route, standalone support surfacing must now fail closed to `Wave 6 bundled install -> Wave 7-8 doctrine-only follow-through`, not `Wave 7-8 full catalog`; keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `SUPPORT_SYSTEM_START_WAVE === 7`, keep `buildWave6ChassisBreakpointChoices(...)` bundling exactly one doctrine-filtered install into the `Wave 6` rider card, keep `getVisibleSupportOfferSystemIds(...)` still narrowing `Wave 7-8` to one doctrine-filtered midrun system, and keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting both the `Wave 6` install and the continued `Wave 7-8` doctrine filter. If a future pass reopens the full support catalog at either beat, the run will drift back into helper-tech breadth before the bundled midrun bracket is proven fun.
- On the shipped `Wave 1-8` route, roadmap/era/feed language must now fail closed to `Wave 8 proof + short victory lap`, never `Wave 9-12 / Act 3 / Crown Break / 12-wave contract`: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) clamping `getBuildRoadmap(...)` and `getForgeEraPlan(...)` to `DEFAULT_ROUTE_WAVE_COUNT` during the consolidated path, keep the final roadmap step on `완성 시험 -> 짧은 승리 랩`, and keep `enterForge()` / consolidated transition feed lines talking only about the current proof and lap. If a future pass lets base-route helper summaries or forge-feed copy mention `Wave 9-12`, `Act 3`, or `12-wave spine` again, the shipped run will drift back into promising an absent arc instead of selling the live close.
- On the shipped `Wave 1-8` route, the recurring forge must now fail closed to `visible gun leap / body-or-defense answer / real risk fork` only; keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) filtering the base-route headline lane through `isBaseRouteHeadlineChoice()` so early headline cards stay on weapon mutations instead of doctrine/core/recipe sidegrades, keep the rider lane suppressing `preview_support` before `SUPPORT_SYSTEM_START_WAVE`, and keep `isBaseRouteGreedChoice()` excluding `reforge` branches so `Wave 5` sells an actual greed bend instead of bench admin. If a future pass lets early wildcard/pivot/catalyst/reforge cards or support-preview cards back into the live `8-wave` forge, the shipped build economy will drift back into taxonomy before one rerun-driving form spike lands.
- On the shipped `Wave 1-8` route, the forge contract must now fail closed to `Wave 1-4 two slots, Wave 5-8 three live lanes`; keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `shouldOpenBaseRouteSecondaryBranch(nextWave)` staying open from `Wave 5` until the late armory handoff so `주력 / 버팀 / 판돈` remains a real rerollable branch instead of a one-stop exception. If a future pass collapses `Wave 6-8` back to two cards, the run will lose the build tension the latest critique called out before longer-route work can matter.
- On the shipped `Wave 1-8` route, `Wave 7-8` must now fail closed to `Dominion Sweep -> Dominion Proof`, not support validation: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on the shared `support_lap/support_proof` cells renamed and rewritten around the same gun/body ownership contract, keep `Wave 7` and `Wave 8` both on `pressureFamily === "domination"` with no relay upkeep, and keep the shipped presentation beats/status strip/smoke expecting `지배 연장`, `Dominion Sweep`, and `Dominion Proof`. If a future pass restores `Support Sweep/Proof` labels or reintroduces support-specific proof copy before the late handoff is actually playable, the domination window will collapse back into helper-tech validation instead of letting the main form breathe.
- On the shipped `Wave 1-8` route, the reward shell must now fail closed to one present-tense focus line plus three hunger cards, not a route brief: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `renderTitleLaunchPanel()` rendering only the silhouette plus `빈 선체 돌입`, keep `createBaseRouteForgeContextMarkup()` emitting only the two-slot `현재 전장 / 현재 형태` strip with no pulse/detail sentence, and keep the base-route forge cards themselves as the only real selling surface. If a future pass restores future-wave title copy or grows the forge header back beyond that two-slot strip, the reward stop has drifted back into plan-reading instead of pick hunger.
- On the consolidated shipped `Wave 1-8` route, `Wave 7` must now fail closed to a first-support ownership lap, not a sudden crossfire jump: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `Wave 7 = Support Sweep` with `pressureFamily === "domination"`, no `lancer`, a larger `1720x960` arena, and only a modest rise from `Wave 6` (`activeCap 20`, `spawnBudget 112`) before `Wave 8` reintroduces the tighter crown-proof ask. If a future pass pushes `Wave 7` back toward `activeCap 26`, shrinks the arena, or restores crossfire/lancer tax as the first rider proof, the route will collapse back into treating support as crisis management instead of a hunger-making ownership spike.
- On the consolidated shipped `Wave 1-8` route, the title and base-route forge must now fail closed to the current state only, not even an immediate next-spike roadmap: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `getBaseRouteTransformationFocus(..., { stage: "title" }) = Bare Hull / 빈 선체 돌입`, keep `renderTitleLaunchPanel()` rendering that same present-tense line, and keep `createBaseRouteForgeContextMarkup()` / `renderForgeOverlay()` on the stripped two-slot header instead of a `forge-focus` card with proof/admin framing. If a future pass restores title roadmap copy, `forge-focus__hint`, or a base-route context card that reintroduces proof rows and route coaching, the shipped UI has drifted back toward briefing instead of hunger.
- On the consolidated shipped `Wave 1-8` route, `Wave 6` must now fail closed to `chassis-plus-one-support` while still keeping the broader catalog dark until `Wave 7`: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `SUPPORT_SYSTEM_START_WAVE === 7`, keep `shouldOpenBaseRouteSecondaryBranch(...)` opening from `nextWave >= 5`, and keep `buildWave6ChassisBreakpointChoices(...)` returning `bayUnlock: true` chassis cards with one bundled doctrine-filtered `systemChoice` instead of body-only placeholders. If a future pass either strips the bundled `Wave 6` install back out or broadly opens standalone systems before the doctrine filter handoff, the shipped route will drift back into either teaser-only support or early spectacle overload.
- On the consolidated shipped route, the honest loop must now fail closed to `Wave 1-8 + short victory lap`, not `Wave 1-6`: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `DEFAULT_ROUTE_WAVE_COUNT === 8`, keep `getShippedRoutePresentationBeats()` / `getBaseRouteTransformationFocus()` / result copy speaking in `Wave 3 weapon break -> Wave 5-6 second axis -> Wave 8 proof -> short lap` terms, and keep `createBaseRouteVictoryLapWave()` piggybacking on the post-`Wave 8` bracket instead of the old `Wave 6` payoff. If a future pass shrinks the honest route back under `Wave 8` or restores `Wave 6` as the end-of-run chassis celebration, the live product has drifted back to hiding the second build axis before the player can actually use it.
- On the consolidated shipped route, `Wave 6` must now fail closed to `chassis lock + bundled support install + greed fork`, not chassis-only austerity: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `SUPPORT_SYSTEM_START_WAVE === 6`, keep `shouldOpenBaseRouteSecondaryBranch(...)` opening both `nextWave 5` and `6`, keep `buildWave6ChassisBreakpointChoices(...)` bundling one real support system into `bastion_bay_forge`, and keep `buildForgeChoices(...)` reserving standalone `system` rider cards for `Wave 7+` so `Wave 6` still reads first as a body lock. If a future pass pushes support back to `Wave 9+`, or lets `Wave 6` rider collapse to a plain defense mod again, the shipped run will lose the first real second-axis proof window that this pass opened.
- On the consolidated shipped route, `Wave 1-2` must now fail closed to one combat lesson at a time before the `Wave 3` weapon break: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `Wave 1 = almost all scuttlers, no hazard` and `Wave 2 = scuttler/brute only, no hazard`, and keep the first live `Cinder Surge` delayed until `Wave 3`. If a future pass reintroduces `shrike` or any hazard on `Wave 2`, or turns `Wave 1` back into a mixed opener, the bare-hull start has drifted back into layered homework before the first earned weapon leap.
- On the consolidated shipped route, `Wave 6 clear` must now fail closed to one short no-admin domination window before results, not straight to the summary: keep `getBaseRoutePostWaveTransition()` in [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) returning `승리 랩` until a `baseRouteVictoryLap` wave has actually been played, keep `beginBaseRouteVictoryLap()` launching one larger open-lane combat cell with no extra forge/objective baggage, and keep HUD/feed surfaces naming that state as `Victory Lap`/`LAP` instead of pretending the run is already over. If a future pass restores direct `Wave 6 -> result` flow or adds another reward/admin stop between chassis lock and the lap, the shipped six-wave spine has drifted back to celebrating the form before the player really gets to use it.
- On the consolidated shipped route, the title and base forge should now fail closed to one appetite-first sentence plus two ultra-short role chips, not a mini roadmap or long lane names: keep [playables/cinder-circuit/index.html](/Users/seren/workspace/poong-game/playables/cinder-circuit/index.html) on the short `빈 선체로 들어가 첫 형태를 훔쳐 나온다.` kicker, keep `renderTitleLaunchPanel()` in [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) rendering only the silhouette, one `Bare Hull -> Weapon Break -> Chassis Lock` headline, and one short `Wave 3 / Wave 6 / 짧은 승리 랩` sentence with no proof-strip pills, and keep the base-route forge contract labels/data on `주력 / 버팀 / 판돈` instead of the longer `주력 변이 / 방호·보조 / 판돈·유틸` admin phrasing. If a future pass restores title beat chips, longer forge lane labels, or another context box that re-briefs the six-wave route before or during the pick, the shipped reward loop has drifted back toward roadmap UI instead of snap desire.
- On the consolidated shipped route, the title opener must now fail closed to a launch panel, not anything named or behaving like signature selection: keep [playables/cinder-circuit/index.html](/Users/seren/workspace/poong-game/playables/cinder-circuit/index.html) mounting `title-launch-panel`, keep `renderTitleLaunchPanel()` in [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) rendering one silhouette, one dominant `Bare Hull -> Weapon Break -> Chassis Lock` headline, and one short `Wave 3 -> Wave 6 -> 짧은 승리 랩` sentence on the shipped route, and keep title-return flows reusing that same panel instead of rebuilding any signature-named surface. If a future pass restores `signature-cards`, clickable title picks, or a multi-card title roadmap under the shipped route, the opener has drifted back into fake choice and briefing admin instead of an appetite-first plunge.
- On the consolidated shipped route, player-facing progression copy must now fail closed to `Wave 1-6` plus a `Wave 6` victory lap, not `prove-out`, `Act 1/2`, `remaining waves`, or `lock then end` framing: keep [playables/cinder-circuit/index.html](/Users/seren/workspace/poong-game/playables/cinder-circuit/index.html) on `lock then sweep`, keep `getBaseRouteTransformationFocus()`, `getShippedRoutePresentationBeats()`, `createShippingLadderMarkup()`, `renderWaveTrack()`, and result-copy in [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) speaking in `weapon break -> chassis lock -> short final lap` terms, and keep the default route name reading as `Bare Hull Run` instead of a generic `Lean Start`. If a future pass restores `Wave 6 clear/prove-out`, `Act 1/2` chips, or `남은 웨이브` language to title/HUD/result surfaces, the shipped route has drifted back into excerpt framing instead of an honest short rerun.
- On the consolidated shipped route, the forge contract should now fail closed to `Wave 1-4 = two cards`, `Wave 5 = one visible second-axis branch`, and `Wave 6 = two cards again for chassis lock`. Keep `buildForgeChoices(...)` and `buildFieldGrantChoices(...)` in [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) opening `주력 + 버팀 + 판돈` only at `nextWave === 5`, keep `preview_support` eligible there so the short route can hint orbitals/halos before the run ends, and keep `Wave 6` collapsing back to `headline + rider` so the chassis break does not become another three-lane admin stop. If a future pass restores `Wave 1-4` or `Wave 6` to full three-card breadth, or removes the lone `Wave 5` greed/support branch again, the six-wave rerun hook has drifted back toward either overbuilt opener noise or a too-linear mid-run.
- On the consolidated shipped route, the opener and any live route-track surface must now fail closed to the same literal `6-wave` loop: keep [playables/cinder-circuit/index.html](/Users/seren/workspace/poong-game/playables/cinder-circuit/index.html) on one lean kicker plus one `title-launch-shell`, keep `renderTitleLaunchPanel()` in [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) rendering only `Bare Hull -> Weapon Break -> Chassis Lock` with `Wave 6 clear`, keep `getShippedRoutePresentationBeats()` as the single player-facing beat source, and keep `renderWaveTrack()` from reintroducing act labels, explanatory notes, or post-capstone nodes on the shipped route. If a future pass restores a title focus card, `Act 1/2` track language, or any `Wave 9+` promise to those default surfaces, the released route has drifted back into roadmap marketing instead of one honest short loop.
- On the consolidated shipped route, the title screen must now fail closed to one plunge-first launch panel instead of a three-card contract briefing: keep [playables/cinder-circuit/index.html](/Users/seren/workspace/poong-game/playables/cinder-circuit/index.html) on one short kicker, one `title-launch-panel`, one primary `회로 투입` CTA, and no extra opener note; keep `renderTitleLaunchPanel()` in [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) rendering the consolidated title as one silhouette frame plus a compact `wave 3 / wave 5 / wave 6` proof strip; and keep the old title-only spotlight path removed so returning to title does not rebuild briefing cards. If a future pass restores multi-card opener coaching or a second title-only info block before the player presses start, the shipped opener has drifted back toward explanation instead of appetite.
- On the consolidated shipped route, the only live `Wave 5-6` forge contract should now fail closed to one real weapon headline, one body/defense rider, and one greed fork: keep `buildForgeChoices(...)` filtering `nextWave >= 5 && nextWave <= DEFAULT_ROUTE_WAVE_COUNT` headline picks through `isBaseRouteHeadlineChoice(...)` so the shipped headline cannot drift into core pivots, support systems, or generic mods; keep the rider lane scored through `scoreBaseRouteRiderChoice(...)` so `Wave 6` prefers the chassis break (`bastion_bay_forge`) over smaller sustain pieces; and keep smoke asserting `Wave 5` headline is `afterglow_mutation`, `Wave 6` headline stays weapon-tier (`evolution` or `wave6_ascension`), and `Wave 6` rider stays `bastion_bay_forge`. If a future pass lets the six-wave forge headline turn back into a pivot/system catalog or lets the `Wave 6` rider stop feeling like a body lock, the shipped build spine has drifted back toward feature-menu breadth instead of one honest weapon/body/greed contract.
- On the consolidated shipped route, run-start signature state must now fail closed all the way through app-state creation, not just title copy: keep `applySignatureToBuild(...)` writing `signatureId === null` under consolidation, keep `createAppState()` / `startRun()` / title returns ignoring `selectedSignatureId`, and keep smoke asserting `createInitialBuild("relay_oath" | "scrap_pact" | "rail_zeal")` all land on the same null-signature `Ember` starter until a real doctrine forecast or lock is earned later. If the shipped route starts carrying a non-null start signature through run creation again, the opener has drifted back toward a hidden class preload instead of one honest lean starter.
- On the consolidated shipped route, live combat HUD should now fail closed to the top survival strip only: keep [playables/cinder-circuit/index.html](/Users/seren/workspace/poong-game/playables/cinder-circuit/index.html) free of the bottom `wave-objective` overlay, keep `updateHUD()` in [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) from rendering combat ask/admin cards during normal waves, keep `Tab` inert instead of opening inspect state, and keep smoke proving `getMinimalBaseRouteHudVisibility(...)` only expands while paused. If a future pass restores a bottom-card contract board or a live `Tab` summary during combat, the shipped route has drifted back toward run administration instead of one-glance survival reads.
- On the consolidated shipped route, the player silhouette must now fail closed to `weapon/body only` until a support system is actually installed: keep `drawPlayerPreviewSupportFrame(...)` absent from the live render path, keep `getPreviewSupportFrameProfile(...)` absent from the exported default-route surface area, and keep smoke proving that polluted/primed shipped builds still show `previewSupportSystemId === null` plus no support stats. If a future pass restores rings, drone fins, or halo arcs around the base chassis before a real install lands, the opener has drifted back toward preview spectacle instead of an earned later reveal.
- On the consolidated shipped route, `Wave 6` must now fail closed to a longer second domination lap instead of an immediate re-tax: keep `WAVE_CONFIG[5]` on `cellId === "payoff_plus"` with no hazard, keep its arena larger than `Wave 5` (`1640x920`), and keep the lane-crossing mix free of `lancer` while pressure rises only slightly from the first payoff (`activeCap 17`, `spawnBudget 92`). If a future pass restores `Crown Breach Relay`, shrinks the arena back under `Wave 5`, or reintroduces dive-tax enemies before the run clear, the first weapon/body payoff window has drifted back from ownership into maintenance.
- On the consolidated shipped route, the first rerunnable gun spike must now fail closed to a visible ship silhouette, not projectile math only: keep `drawPlayerWeaponFrame(...)` live in [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) before doctrine/support/late frames, and keep it rendering core-specific barrel/prism/furnace/rail geometry off `state.weapon.core.id` plus `max(evolutionTier, doctrineStage)`. If Wave 3 pivots or Wave 5 weapon growth stop changing the player frame and only widen bullets again, the opener has drifted back toward invisible spreadsheet power instead of a rerunnable `I want that form again` jump.
- On the consolidated shipped route, `Wave 5` must now fail closed to the softest open-lane payoff in the six-wave prove-out: keep its shipped config on `SECOND_ACT_PAYOFF_ARENA`, keep `WAVE_CONFIG[4].hazard === undefined`, and keep its pressure lighter than `Wave 6` (`activeCap 16`, `spawnBudget 88`, slower cadence, longer duration) so the first weapon/body spike gets a real `I own this lane now` lap before the clear asks for more. If a future pass puts a bastion/relay objective, tighter arena, or equal-or-higher density back onto `Wave 5`, the first payoff has drifted from readable mastery back into chores.
- On the consolidated shipped route, the title card and base forge context should now fail closed to one shared `next visible transformation` grammar: keep `getBaseRouteTransformationFocus(...)` driving the wide title focus card and `createBaseRouteForgeContextMarkup(...)`; keep base forge context on `eyebrow + transformation title + one short hint`; and keep `다음 시험`/proof pills out of that base forge header. If the default route starts foregrounding signature picks, proof windows, or support/admin hints on those surfaces again, the opener has drifted back into route paperwork instead of `survive -> transform`.
- On the consolidated shipped route, the opener must now fail closed to one universal lean starter gun before the first forge leap: keep `LEAN_START_CORE_ID === "ember"`, keep `applySignatureToBuild(...)` forcing that core whenever `CONSOLIDATED_12_WAVE_ROUTE` is true, keep the title screen rendering the three-step lean-start contract instead of clickable signature cards, and keep title-screen digit hotkeys dark so the player cannot secretly preload `relay_oath / scrap_pact / rail_zeal` before moving. If the opener ever returns to visible signature picking or starts `ricochet / scatter / lance` again on the shipped route, the early growth curve has drifted back toward a visually solved opener instead of an earned Wave 3-6 transformation ladder.
- On the consolidated shipped route, the player-facing run contract must now fail closed to a `Wave 1-6` prove-out: keep `DEFAULT_ROUTE_WAVE_COUNT === 6`, keep consolidated `ACT_LABELS` capped at `Act 1 / Act 2`, keep `renderWaveTrack()` slicing the track to six nodes, keep `beginWave()` marking `Wave 6` as `completesRun`, and keep `getShippingLadderSteps(...)` at three beats only `START -> 도약 -> 방호`. If the default route starts surfacing `Wave 7-12`, `Act 3`, or a `점화` step in the shipped HUD/track/ladder again, the run has drifted back toward a longer promised contract before the short spine is proven replayable.
- On the consolidated shipped route, `Wave 1-6` pressure must now fail closed to a calmer hunger-to-domination ramp: keep `WAVE_CONFIG` opener caps at `14 / 16 / 19 / 22 / 16 / 17`, keep `Wave 5` and `Wave 6` spawn budgets down at `88 / 92`, and keep the run clear free of new hazard lockouts or `lancer` tax so the first weapon/body break reads as space ownership instead of crowd compression. If a future pass pushes the shipped opener back toward `Wave 5-6` crowd spikes or restores dive-tax pressure before the six-wave clear, the prove-out has drifted back toward stress without payoff.
- On the consolidated shipped route, contraband overclock must now fail closed so `Dominant Mutation` stays a pure main-weapon payoff: keep `shouldAllowContrabandOverclockRoute()` false with `CONSOLIDATED_12_WAVE_ROUTE`, keep `createIllegalOverclockChoices(...)` / `createIllegalOverclockMutationChoice(...)` returning no shipped-route picks, and keep `applyForgeChoice(... risk_mutation ...)` from auto-bundling `illegalOverclockId` or `illegalOverclockMutationLevel`. If a future pass makes `risk_mutation` advertise `contraband splice 동봉`, or a default-route smoke can again install `Glass Broadside` / `Meltdown Cycler` / `Rupture Crown`, the opening ladder has drifted back toward side-fantasy clutter instead of one weapon/body climb.
- On the consolidated shipped route, `Wave 5-12` should now fail closed to one explicit pressure ladder `claim_space -> hold_breach -> survive_spike`: keep `buildRecurringCombatWave(...)` stamping `ladderStepId/ladderStepLabel`, keep `LATE_BREAK_ENCOUNTER_PROFILES` / `LATE_BREAK_FOLLOWTHROUGH_PROFILES` deriving `Wave 9-10` from `SHARED_LATE_ACT_ENCOUNTER_POOL` through `buildLateBreakLadderWave(...)`, and keep late proof summaries surfacing those ladder labels in the detail line. If branch-specific `Wave 9-10` objects start hand-authoring their own note/directive grammar again or late proof text stops saying `Claim Space` / `Hold Breach` / `Survive Spike`, the route has drifted back toward authored scenario branches instead of one reusable open-arena rung set.
- On the consolidated shipped route, `Wave 5-8` forge contracts must now fail closed to `주력 변이 / 방호·보조 / 판돈·유틸` as three genuinely different appetites: keep the rider slot filtering to chassis or explicit defense pieces only (`bastion_bay_forge`, `thermal_weave`, `armor_mesh`, `step_servos`, `coolant_purge`), keep the gamble slot filtering to simple economy/bench churn only (`field_greed`, `recycle`, `reforge`, `affix_reforge`, `salvage_link`, `magnet_rig`, `reactor_cap`), and keep smoke locking that `Wave 5`, `Wave 6`, and `Wave 8` do not surface systems, pivots, or generic sustain in those lanes. If rider/gamble starts pulling from the same sustain bucket again, the early forge has drifted back into a feature menu instead of one main spine plus a restrained defense and greed hook.
- On the consolidated route, `Wave 5-10` hazard/band names must now fail closed to the same shared payoff/proof grammar the wave titles already use: keep `Wave 5` on `Payoff Bastion`, `Wave 6` on `Crown Breach Relay`, `Wave 8/10` on `Crown Proof Relay`, and keep late-break branch overrides reusing `bandId === "late_payoff_run"` / `bandId === "crownhold_proof"` with `bandLabel === "Payoff Run+"` / `Crown Proof+`. If branch-specific names like `Overdrive Payoff`, `Halo Proof`, or `Crownhold Relay` return on the shipped `Wave 5-10` route, the midgame has drifted back toward itinerary flavor instead of one reusable escalation ladder.
- On the consolidated shipped route, default live combat should now fail closed to survival meters plus one `현재 전장` ask card: keep `.build-panel--right` entering `hud-build-panel--solo`, keep the live DOM free of `active-core / pending-cores / build-roadmap / live-readout / upgrade-list`, and keep `waveObjective` rendering either the minimal ask card or the `Tab` inspect board instead of opening a second combat rail. If `현재 형태`, `다음 변화`, or another bottom-card stack reappears during normal waves, the arena has drifted back toward dashboard/admin reads instead of the `Hades`/`Nova Drift` one-ask combat surface.
- On the consolidated shipped route, primer/wildcard legacy state must now fail closed even if an old build is polluted: keep `sanitizeConsolidatedBuildState(build)` clearing `previewSupportSystemId` and `wildcardProtocolIds`, keep `buildForgeChoices(...)`, `applyForgeChoice(...)`, and support-stat helpers calling that scrubber, and keep smoke proving a manually polluted `Wave 5` build cannot resurrect `preview_support` or `wildcard_protocol`. If future helpers read those fields directly again without the scrubber, the build spine can quietly widen back into pre-takeover support/contraband noise.
- On the consolidated route, `Wave 11-12` should now fail closed to the shared late cells `Refuge Run -> Final Stand`: keep `WAVE_CONFIG[10].label === "Wave 11 · Refuge Run"` and `WAVE_CONFIG[11].label === "Wave 12 · Final Stand"`, keep the shared late pool using `bandId === "refuge_run"` / `bandId === "final_stand"`, and keep late-route summaries/proof windows echoing those generic cell names instead of `Starforge Pursuit` / `Cinder Bastion`. If any default route preview, track, or summary revives the old names, the back half has drifted back toward itinerary tourism instead of reusable escalation grammar.
- On the consolidated shipped route, the `Wave 5` early stop must now live under `forgeDraftType === "early_mutation"` instead of the legacy `field_grant` draft type: keep `getForgeDraftType({ nextWave: 5, ... }) === "early_mutation"`, keep `enterFieldGrant()` rewriting the shipped stop to that mode, and keep live readout / instant-draft handling recognizing `early_mutation` as the same appetite-first forge beat. If base-route `Wave 5` falls back to `field_grant` labels again, the reward spine has drifted back toward cache/admin plumbing instead of one clean forge-only contract.
- On the consolidated shipped route, base forge cards should now fail closed to `tag + title + one fantasy line + cost`, with `다음 시험` living only in the quiet context card above them. Keep `createBaseRouteForgeHeadlineCardMarkup(...)` and `createBaseRouteForgeCompactCardMarkup(...)` rendering `forge-card__hero-copy` from `transformation.promise`, and fail closed if preview pivots, proof capsules, impact strips, or extra slot-detail scaffolding reappear inside the cards. If the reward pause starts asking the player to compare rows again instead of reading one fantasy line and one bill line, it has drifted back away from the appetite-first `Hades`/`Nova Drift` snap.
- On the consolidated route, `Wave 9` should now be a true late-form `Payoff Run+` instead of another sweep/gimmick handoff: keep `WAVE_CONFIG[8].label === "Wave 9 · Payoff Run+"`, keep the base/shared/late-break `Wave 9` configs on `pressureFamily === "domination"` with no player-facing hazard structure, and keep `renderWaveTrack()` showing `bandLabel/label` headlines rather than internal ids like `LOCKGRID` or `AFTERGLOW`. If `Wave 9` regrows surge/territory/salvage gimmicks or the track starts surfacing code names again, the route has drifted back toward named-tour administration instead of `forge -> payoff lap -> proof`.
- On the consolidated shipped route, the base forge reward surface should now fail closed to one dominant headline card plus two compressed side cards: keep `createBaseRouteForgeCompactCardMarkup(...)` reserved for `방호·보조 / 판돈·유틸`, keep all three cards on the same `title + fantasy line + cost` grammar, and keep `.forge-card--contract[data-contract="headline"]` spanning the wide slot. If rider/gamble cards regrow preview rows, proof rows, or equal explanatory scaffolding beside `주력 변이`, the reward pause has drifted back away from the `Hades`/`Nova Drift` appetite hierarchy this pass restored.
- On the consolidated shipped route, live combat reward drops must now fail closed through `shouldAllowCombatRewardDrops() === false`: keep wave construction from arming `combatCache`, `lateAscension`, or `doctrineAscension`, and keep `deployCombatCache(...)` / `deployLateAscension(...)` returning early so elite kills cannot reintroduce arena-paid growth on `Wave 1-12`. If a future refactor reuses those shared deploy helpers without the guard, the reward spine will drift back away from `fight -> forge -> visible transformation -> proof`.
- On the consolidated shipped route, `Wave 5` should now resolve through the same between-wave forge cadence as the rest of `Wave 1-12`: keep `getBaseRoutePostWaveTransition(..., 5).action === "forge"`, keep `getCombatCacheChoicesForWave(build, 5+) === []`, and fail closed if stale `combat_cache` / `late_ascension_cache` drops survive into the default path. If a future pass reopens any player-facing cache stop or lets `Wave 4-12` spawn live cache pickups again, the reward spine has drifted back away from `fight -> crave forge -> transform -> test`.
- On the consolidated route, the default combat/forge surface should now collapse to one build headline and one immediate ask: keep minimal combat HUD from surfacing `다음 변화` or a wave-name hero card, and keep `createBaseRouteForgeContextMarkup(...)` to `title + 다음 시험` only with no rider pill or explanatory prompt. If the shipped default HUD/forge starts reintroducing `보조 결`, wave labels, or sentence-length setup copy on the main surface, the run has drifted back toward admin framing instead of appetite-first reads.
- On the consolidated route, `Wave 5-8` `주력 변이` should now score for visible transformation first instead of just taking the first sorted candidate; keep `buildForgeChoices(..., { nextWave: 5-8 })` preferring `afterglow_mutation`, `crownfire_overdrive`, weapon evolutions, or a real core pivot before affix/mod upkeep. If a capped mid-run build goes back to showing `Overclock` or other small tuning cards in the headline slot while a core swap is available, the takeover arc has flattened back into maintenance instead of appetite.
- On the consolidated route, `lateBreakProfileId` should now own only `Wave 9-10`; `Wave 11-12` must fall back to the shared `Refuge Run -> Final Stand` spine even for `mutation`, `aegis`, and `ledger`, with branch identity surviving in `Wave 9-10` pressure seasoning and build headlines instead of bespoke finale scenarios. If `getDirectLateBreakWaveOverride(...)` starts overriding index `10+` again or late-break HUD summaries revive `Crownbreaker/Halo Refuge/Kingpin` labels for `Wave 11-12`, the route has drifted back toward branch tourism instead of reusable late combat bands.
- On the consolidated `12-wave` route, post-wave routing should now fail closed through `getBaseRoutePostWaveTransition(...)`: every non-final clear, including `Wave 5` and `Wave 9`, must resolve to a forge beat, with `Wave 9` specifically returning `action === "forge"` and label `마무리` even if stale `wave6ChassisBreakpoint` or `doctrineChaseClaimed` state survives on the build. If a future pass reopens live-ascension, ownership-skip, or other in-combat routing exceptions on the shipped path, the reward cadence has drifted back away from `fight -> forge -> test`.
- On the consolidated route, `Wave 7` `crownfire_overdrive` must stay a real visual takeover, not just a named stat bump: keep its per-core defs visibly larger than `afterglow`/`evolution t1` (`ember` flank squadrons, `scatter` outer pellet ring, `lance` six side rails, `ricochet` six-way prism fan), and keep smoke locking that `rail_zeal` resolves `Wave 7` headline to `Sky Splitter` with the widened six-rail fire pattern while `Wave 8` still withholds standalone support hardware. If `crownfire` falls back to four side shots or a tiny invisible DPS bump, the main-form spine has flattened again before the delayed rider reveal.
- On the consolidated shipped route, `Wave 8-12` must not reopen `Field Cache` or any other `combat_cache` handoff; keep `shouldUseFieldGrant({ nextWave: 9+ }) === false`, keep `getCombatCacheChoicesForWave(build, 9+) === []`, and let late growth come only from between-wave forge picks. If `Wave 8` starts promising `Combat Cache` or a late wave spawns field drops again, the run has slipped back into arena-delivered progression instead of anticipation between fights.
- On the consolidated shipped route, future-hardware helpers must now fail closed even if old state survives on the build: keep `getClaimedWildcardProtocolIds(build) === []`, `getPreviewSupportSystemId(build) === null`, and `applyForgeChoice(...wildcard_protocol)` inert under consolidation, and keep smoke coverage asserting that `previewSupportSystemId` / `wildcardProtocolIds` do not reanimate support primers or contraband branches in `Wave 1-10`.
- On the consolidated shipped route, standalone support hardware must now stay dark through `Wave 8`: keep `BASE_ROUTE_MIDRUN_SUPPORT_WAVE === SUPPORT_SYSTEM_START_WAVE`, keep `SUPPORT_SYSTEM_DEFS[*].forgeWaveMin === 9`, keep `buildForgeChoices(..., { nextWave: 8 })` from surfacing system installs in the recurring contract, and make wildcard schedule helpers return `null` under consolidation so `Wave 7-10` cannot quietly re-advertise primer/contraband beats through shared helpers or future UI.
- On the consolidated route, `Wave 5-10` should now be authored through the shared `buildRecurringCombatWave(...)` helper and the four reusable cells `Payoff Run / Crown Breach / Payoff Sweep / Crown Proof`, with late shared-pool `Wave 9-10` reusing the same grammar. If bespoke names like `Afterglow`, `Breakline`, `Crownfire`, `Forgecross`, `Lockgrid`, or `Crownhold` retake the player-facing labels or those waves grow custom note/directive logic outside the helper again, the midgame has drifted back toward scripted scenario tourism instead of a repeatable combat ladder.
- On the consolidated route, late-break `Wave 9-10` should now stay a literal shared cell `branch payoff -> crownproof` even when the branch fantasy differs; `mutation`, `aegis`, and `ledger` can keep different `Wave 9` asks, but `resolveWaveConfig(9, build)` should land on a `breach` proof with `relay` hazard for all three. If `Halo Refuge` drift or `Jackpot Caravan` chase comes back at `Wave 10`, Act 3 has drifted back toward branch tourism instead of one reusable proof rung.
- On the consolidated route, late-break `Wave 9-10` player-facing labels should now clamp back to the shared cell names `Payoff Sweep+ -> Crown Proof+` for all three late-break lanes; keep branch identity in band labels, enemy mix, and hazard punctuation instead of letting `Halo Bastion`, `Ledger Vaultline`, or other bespoke titles retake the main wave header.
- On the consolidated route, `Wave 7` headline should now be a second unmistakable main-form leap after the `Wave 6` chassis lock, not just `weapon evolution t2` on rails; `buildForgeChoices(..., { nextWave: 7 })` should surface a `crownfire_overdrive` headline once `afterglow` or `evolution t1` plus a chassis break are live, and `Wave 8` should then fall back to the next weapon evolution while standalone support stays hidden until the late-form handoff. If `Wave 7` goes back to a plain `evolution 2` ladder for every run, the mid-run contract has drifted back into a scripted staircase instead of a reusable `weapon/body spike -> delayed support payoff` cell.
- On the consolidated route, visible standalone support installs must stay fully dark for every build until `Wave 9`; `Ember Ring`, `Aegis Halo`, and `Volt Drones` should all keep `forgeWaveMin === 9`, and `getVisibleSupportOfferSystemIds(build, 8)` should return `[]` even with a doctrine lock. If a generic `Wave 8` rider can install orbitals, halos, or drones again, the recurring `주력 변이 / 방호·보조 / 판돈·유틸` contract has leaked spectacle back in before the late-form handoff lands.
- On the consolidated route, `Wave 7-8` should keep the three-lane draft but hide real support installs entirely; the rider lane should resolve to chassis-defense/utility cards there, and the first visible standalone system silhouette should only appear from `Wave 9`. If `getVisibleSupportOfferSystemIds(build, 8)` starts returning drones/orbitals/halos again, the mid-run transformation arc has flattened back into early visual completeness.
- On the consolidated route, `Wave 4-10` `판돈·유틸` should no longer auto-resolve to the same `Greed Contract` every stop; the third lane lands better when base-route forge scoring can surface `recycle / reforge / magnet / mobility` utility plays on healthy runs and only let raw greed retake priority when scrap is low or the late act is about to cash it in. If base-route gamble picks flatten back into one fixed scrap-debt card every wave, the three-lane draft has drifted back into a disguised schedule instead of a rerunnable offense/defense/utility split.
- On the consolidated route, `Tab` inspect lands better as a `hero card + three value lanes + one tiny hint chip` board in the spirit of `Brotato`/`Nova Drift` status reads; if lane paragraphs, stacked notes, or document-style explanations come back under `주력 변이 / 방호·보조 / 판돈·유틸`, inspect has drifted back into a build sheet instead of a glance board.
- On the consolidated route, the live two-card HUD reads cleaner when the left growth card says `현재 형태 + 다음 변화` and the right combat card resolves to `wave title + one short ask + one small threat pill`; if `다음 도약`, proof jargon, or multi-row status/admin copy comes back onto those default cards, the combat surface has drifted back toward prototype briefing.
- On the consolidated shipped route, any player-facing act label, late cache helper, or side-bet summary must hard-clamp to `Wave 12`; if `getActLabelForWave(13+)` returns `Act 4`, `getCombatCacheChoicesForWave(..., 14+)` surfaces post-capstone choices, or end-of-run copy hints at a later escalation, the current release path is still advertising a cooler game after the clear.
- On the consolidated route, the default combat HUD should now stay a literal two-card read: left `현재 형태` headline card with one `다음 변화` band, right `현재 전장` ask card; `build-roadmap`, `live-readout`, `upgrade-list`, and bench chips belong to `Tab`/pause only. If the left card disappears again in live combat or roadmap/live telemetry re-enter the default wave view, the shipped HUD has drifted back toward dashboard mode.
- On the consolidated route, `Wave 5-12` battlefield reads should now pull from one terse directive line, not the long authored `note`; keep the HUD `현재 전장`, wave-start feed, and wave track on that short ask or the shipped run drifts back into narrated route vocabulary instead of readable combat pressure.
- On the consolidated shipped route, `Wildcard Protocol` should no longer surface at `Wave 4 / 7 / 10`; if field grants or gamble slots reintroduce `Smuggler Winch`, `Nullplate Halo`, or `Rogue Lattice` before the main late-form lock, the run has drifted back toward exception shopping instead of three clear gun/body destinies plus one light rider.
- On the consolidated route, `Tab` inspect should now be a quick status board: one hero card `현재 형태 + 다음 전장 + 보유 고철`, then exactly three short lanes `주력 변이 / 방호·보조 / 판돈·유틸`; if inspect falls back to the old compact focus card, reopens bench/live telemetry, or drops back to equal-weight roadmap/admin rows, shipped readability has drifted back toward a document instead of a glanceable board.
- On the consolidated route, `Wave 5-8` should now read as `open payoff -> single breach -> harder open-lane remix -> final breach`, with hazard grammar clamped to `territory -> relay -> none -> relay`; if `Wave 7` regrows drift/territory upkeep or `Wave 5-8` starts flipping through three hazard families again, Act 2 has drifted back toward scripted encounter tourism instead of one readable midform mastery ladder.
- On the consolidated route, the shared focus surfaces should now sell one oversized mutation title first, then one `다음 전장` proof band, then at most one small rider/resource pill; if the base-route HUD focus card or forge context regrows equal-weight triads like `현재 형태 / 다음 전장 / 보유 고철`, the reward/combat hierarchy has drifted back toward three competing primaries.
- On the consolidated shipped route, `Wave 5` should no longer attach any `Ember Ring / Aegis Halo / Volt Drones Primer` preview state at all; if `previewSupportSystemId`, preview orbit stats, or preview combat frames become live again before `Wave 7`, the opener has drifted back toward early visual completeness instead of holding support appetite for the first real midrun install.
- On the consolidated shipped route, `Wave 7-8` support should now be the first real doctrine-aligned install at tier 1, not a discounted `Mk.II` primer completion; if `Volt Drones Mk.II`, `Aegis Halo Mk.II`, or `Ember Ring Mk.II` is again the first visible support pickup after a quiet `Wave 5`, the support ladder has jumped ahead of the weapon/body spine again.
- On the consolidated route, player-facing act chips/track labels should now clamp at `Act 3`/`Wave 12` even if internal post-capstone content still exists for future testing; if shipped HUD, forge chips, or inspect summaries can ever surface `Act 4 · Afterburn` again before a non-consolidated mode is explicitly active, the run has started advertising a later "real game" again.
- On the consolidated `Wave 1-12` route, the forge pause lands cleaner when the base contract hides the header block entirely and the shared context compresses to one horizontal triad `현재 형태 / 다음 전장 / 보유 고철`; if a shipped base-route reward regrows `WAVE BREAK`, subtitle coaching, or a stacked context card above the picks, the pause has drifted back into briefing instead of desire.
- On the consolidated route, `Wave 7-8` recurring forge contracts should now reserve exactly one visible doctrine-aligned support silhouette before the `Wave 9` finale lock: `Mirror Hunt -> Volt Drones`, `Kiln Bastion -> Aegis Halo`, `Storm Artillery -> Ember Ring`. If those midrun contracts fall back to only sustain/wildcard cards again, the payoff ladder has collapsed back into `gun -> chassis -> finale` with no visible support rung.
- On the consolidated route, the base forge reward snap lands better when the overlay header itself goes quiet and the cards stop advertising their lane in text; keep the player-facing base-route pause at `변이 선택` plus the shared context card `현재 형태 / 다음 전장 / 보유 고철`, then render each card as only `title + immediate payoff + one preview row + cost`. If `WAVE BREAK`, subtitle coaching, or `주력 변이 / 방호·보조 / 판돈·유틸` tag pills creep back onto base-route cards, the reward moment has drifted back toward briefing instead of appetite.
- On the consolidated route, `Wave 5` should now surface one core-specific `Afterglow` small mutation before the `Wave 6` chassis lock; if the shipped headline at `nextWave === 5` falls back to a generic evolution/mod card or the weapon summary ignores the extra afterglow barrel/prism pattern, the six-beat payoff spine has flattened back into `weapon break -> chassis break`.
- On the consolidated route, base forge cards land better when their preview row prefers the visible mutation itself `보조 레일`, `격자 레일`, `고철 +회수`, `섀시 이름` over generic carrier labels like `Lance`, `현재 무기 직접 강화`, or branch/admin nouns; if the shipped three-card read falls back to those middle-layer terms again, the reward moment has drifted back into data parsing instead of appetite.
- On the consolidated `12-wave` route, any finale preview, roadmap clamp, or wave-track summary must stop at `Wave 12`; if forge previews say `7연전 시작`, the track grows an `Act 4`, or any shipped guard re-allows `Afterburn/Dominion/finale mutation` offers, the run is implying its real climax still lives after the clear.
- On the consolidated `12-wave` route, doctrine support timing should now stay staggered: `Wave 7-8` exposes one smaller second-family silhouette, while `Wave 9+` returns to the doctrine's one primary late amplifier family. If the shipped path skips the midrun silhouette entirely or starts mixing multiple late families/wildcards back into the same run, the support ladder has drifted again.
- On the consolidated route, `Wave 6` chassis picks land better when the chosen body posture is immediately live for the first seconds of the next fight; if body breaks only change hidden stats and wait for later support/payoff layers to become visible, the `weapon leap -> body commitment` rung still feels theoretical.
- On the consolidated route, the default combat HUD should now demote `현재 형태` fully into `Tab`/forge and leave live combat with only survival meters plus one `현재 전장` card; if the left build panel, roadmap, or upgrade recap reappears during normal waves, the arena has drifted back into dashboard mode.
- On the consolidated route, `Wave 8 -> Wave 9` should surface as one explicit `마무리` beat, not as `Late Break Armory`, `Aux Bay Uplink`, or any other admin unlock; late support capacity can move backstage, but the player-facing handoff must read as `pick one finale form, then prove it through Wave 9-12`.
- On the consolidated route, base forge cards should stay three equal appetite tiles with the same `tag -> title -> immediate change -> cost` stack; if a shipped `Wave 1-12` reward reintroduces `추천`, featured double-width treatment, proof paragraphs, impact strips, or asymmetric preview scaffolding, the reward snap has drifted back into prototype briefing.
- On the consolidated route, the player-facing `런 실루엣` ladder should stay a four-beat appetite template only `약한 시작 -> 도약 -> 방호 -> 점화`; keep the `Wave 5` micro-forge real in progression, but do not advertise it as a separate public rung on HUD/inspect/forge summary cards or the run regains its short predetermined feel.
- On the consolidated route, `Tab` inspect and the forge context should now share the same quiet contract `현재 형태 / 다음 전장 / 보유 고철`, and inspect should hide the spare bench panel instead of opening a second bookkeeping board; if `비용·대가`, stage chips, or side-bet/admin summaries retake those default surfaces, the shipped route has drifted back into briefing mode.
- On the consolidated route, base forge cards should now derive their hero copy from one visible preview row plus one next-fight proof, not from the underlying admin description text; if `doctrine`, `forecast`, `ascension`, `armory`, `bay`, `Afterburn`, or similar backstage nouns reappear in the default `Wave 1-12` card body, the shipped reward snap has leaked back into route administration.
- On the opener, `Wave 1-4` should now breathe through stepped arena growth `1080x620 -> 1160x660 -> 1220x700 -> 1260x720` with softer active caps `16 / 19 / 23 / 28` and only one `Meltdown Surge` lane at `Wave 4`; if the first act shrinks back to the default `960x540`, restores `32` active enemies, or reintroduces double-surge overlap before the `Wave 5` pause, the run has slipped back into prototype compression.
- On the consolidated route, `Wave 4` should now open one bounded `Wave 5` micro-forge pause before the chassis lock: exactly one `주력 변이 / 방호·보조 / 판돈·유틸` pick that bends the freshly earned gun without unlocking support spectacle early. If `Wave 5` falls back to a plain wave transition or regrows combat-cache/admin framing, the mid-run ladder has thinned out again.
- On the consolidated route, the base forge pause lands better when all three cards share equal weight and the context card only shows `현재 형태 / 다음 전장 / 보유 고철`; if `추천`, labeled `다음 시험`, or labeled `비용·대가` rows come back on the default three-card reward read, the reward moment has drifted back into briefing mode.
- On the consolidated route, the minimal combat HUD should now hide both the top `Wave` chip and `Dash` chip; the current wave identity belongs in the single `현재 전장` threat card, not as extra top-strip telemetry beside the survival meters.
- On the consolidated route, if `build.lateBreakProfileId` is set then `resolveWaveConfig` should jump straight from `WAVE_CONFIG` into the branch-owned `Wave 9-12` overrides instead of inheriting `SHARED_LATE_ACT_ENCOUNTER_POOL` first; otherwise offense/defense/greed finales drift back toward one shared staircase with renamed hazards.
- On the consolidated route, the default combat HUD should now collapse to survival meters only during normal play; if timer, scrap, roadmap, bottom-card asks, inspect summaries, upgrade logs, or live readout panels come back during combat, the shipped screen has slipped back into operator-dashboard mode.
- On the consolidated route, the forge header lands better as one quiet context card `현재 형태 / 다음 전장 / 보유 고철` above the three appetite cards; if the base forge context grows another roadmap, support summary, or `다음 점화` coaching row, the reward pause has drifted back into route briefing.
- On the consolidated route, the base forge header should not keep a separate mode chip or extra subtitle coaching; if the shipped `Wave 1-12` forge says more than current form, next battlefield, and scrap, the reward pause is explaining itself again instead of selling the pick.
- On the consolidated `12-wave` route, `getBuildRoadmap` stage II/III copy should never mention `contraband salvage`, `pursuit`, `live ascension cache`, or `Afterburn`; the shipped roadmap lands better when it only points at `next big weapon/body leap` and the fixed `Wave 9-12` payoff/proof/cadence/finale arc.
- On the consolidated route, the default HUD card, `Tab`, and forge focus should all sell the same four-beat spine `약한 시작 -> Wave 3 무기 도약 -> Wave 6 방호 약속 -> Wave 8 후기 점화`; if `proof`, `contract`, or `draft` retake those default surfaces, the shipped run has slipped back into route explanation instead of a closed 12-wave appetite loop.
- On the opener, signature selection lands best when both the card and spotlight read as `label + start core + one bias line`, and the first combat feed is only `start core 기동`; if perk paragraphs, seed/start admin boxes, or launch-brief copy regrow, the opening has slipped back into admin mode before Wave 1.
- On the consolidated route, the best `UX/UI` contract is now literal: wave-clear feed, `Tab`, and the forge focus card should each show only `현재 형태 / 다음 전장 / 보유 고철`; if support pills, roadmap prompts, draft-stage names, or side-bet labels creep back onto those default surfaces, the shipped run has slipped back into briefing mode.
- On the consolidated route, `Wave 3` lands better when the card, roadmap rung, and confirm feed all headline the immediate weapon leap `Prism Crown` / `Twin Spine` style rather than the backstage doctrine name; if the first stop reads `Doctrine`, `Forecast`, or `Core Lock` first, the opening growth beat has drifted back into scaffolding.
- On the consolidated route, `Wave 8` late-break picks land better when each card sells an immediate transformed form and the HUD/feed echoes that form directly; if `Cataclysm Arsenal`, `Warplate Halo`, or `Black Ledger Heist` fall back to bay/admin wording instead of `weapon/body now -> Wave 9 proof now`, the midgame contract has drifted back into infrastructure.
- On the consolidated route, forge reward cards land better when the base three-card contract shows only `what changes now` and one raw cost line; if compact or featured cards regrow proof paragraphs, preview grids, impact strips, or side notes, the reward snap has slipped back into briefing mode.
- On the consolidated route, `Tab` inspect and compact forge focus land better as `headline title + next proof capsule + one bill/resource pill row`; do not let those pauses regress into three parallel lane notes or schema explanations.
- On `ledger` late waves, greed only reads differently if missed vault/caravan cash-outs create immediate combat debt; keep `Debt Surge` tied to missed payout windows instead of passive text so `Wave 9-12` actually punishes over-chasing or botched exits.
- On the consolidated route, wave-clear handoff copy and forge headers should always resolve to `주력 변이 / 방호·보조 / 마무리`; if a base-route banner/readout says `Core Lock`, `Late Form`, `Bastion Draft`, or `Live Ascension`, the player-facing contract has slipped back into admin language.
- On the consolidated `Wave 1-12` route, keep `overcommit`, doctrine chase, live ascension, and catalyst-crucible late-form detours disabled; `Wave 8` should lock one late-break profile and `Wave 9-12` should prove that fixed spine without extra admin exceptions.
- On the consolidated opener, signatures must now fail closed to `start core only`; do not seed a duplicate core, a free affix, opening resources, or any drive/cooling/salvage stat nudge unless a later critique explicitly says the first two waves are too dry.
- If `Wave 3` or `Wave 6` card text mentions support bay, flex lane, shard chase, or capstone payoff in sentence one, the opening is still explaining the route too early; keep those cards focused on `gun now` and `body now`.
- On the consolidated route, `Wave 3 Core Lock` should only forecast a doctrine and grant a visible gun mutation; do not let forecast state apply doctrine stats/fire geometry before `Wave 6 Ascension` actually locks the form.
- If the start screen or result summary starts implying signatures still grant passive opener bonuses, scrub that copy before adding any new early reward wrapper; the shipped route now expects the first visible power spike to come from forge, not preload.
- On the consolidated route, `Wave 12` lands better as a rotating bastion stand than a second relay breach; keep `Wave 9-12` reading `open-lane payoff -> breach proof -> pursuit/refuge -> final stand`.
- Prefer one meaningful gameplay or readability improvement per run.
- Avoid adding another named system if the same benefit could come from simplifying or deepening an existing one.
- Validate every gameplay/UI change with the existing smoke test when relevant.
- On the base `12-wave` route, keep shipping-beat labels internal only; the player-facing forge/HUD should read with generic appetite-first Korean surfaces like `변이 선택`, `보조 선택`, `마무리`, plus the stable three-card grammar `주력 변이 / 방호/보조 / 판돈/유틸`.
- On the consolidated route, the best readability move is now `survival strip live, recap only on pause`; hide bench/upgrades/live telemetry by default and let pause carry the extra detail.
- On the consolidated route, `Wave 1-8` should now stay on one recurring three-lane forge contract; do not reintroduce `Architecture Draft`, `Act Break Cache`, field-grant skips, or two-step package flow before `Wave 9`.
- The base-route HUD/forge contract lands better when it speaks only `Current Form`, `Main Leap`, and `Next Proof`; support should read as a subordinate rider note, not a co-equal reward pillar.
- Standard `Wave 9-10` improvements are highest value when they create a clearly different movement ask with the same locked form; `Wave 10` can carry a mobile greed/breach split without adding a whole new progression system.
- Base-route `Wave 11` is higher value as a pursuit/refuge cadence than another gallery lap; use drift-driven reentry pressure to force pocket-to-pocket rotation before the final breach.
- The fallback late shared pool must mirror the shipping cadence too: `Wave 9` should be an ownership/payoff lap, `Wave 10` a greed/breach split, `Wave 11` a pursuit/refuge rotation ask, and only `Wave 12` the final lane-specific cash-out, not a dump of every hazard family at once.
- Base-route `Wave 12` lands better when it stays a single crownline breach with a slightly larger arena, lower active cap, and lighter binder/mortar tax; difficulty should come from holding one opened corridor, not restoring every closure family at once.
- Late Break Armory cards are stronger when they preview `Wave 11` and `Wave 12` by name; the lane should read like a future payoff the player can chase, not just a stat package.
- Early `Wave 3-6` staging still has more leverage than new branches: lock the core gun first, add chassis second, and keep only a small support silhouette for `Wave 7-8` before the late-form lock unless a later critique proves the base ladder is still starving.
- If `Wave 6` stops offering a chassis-flavored defense card inside the recurring contract, the early ladder has slipped back toward generic stat filler; keep that slot visibly body-plan focused even while the overall forge grammar stays consistent.
- If `Wave 3` doctrine cards still name drones, sentries, missiles, bays, or reserve lanes in sentence one, the opening fantasy is still upside down; move support preference text to post-`Wave 8` support install copy instead.
- On the shipped `Wave 1-8` route, `Wave 6` chassis picks should now stay lean through `Wave 1-5` but bundle one doctrine-linked support rider at the breakpoint itself: keep [playables/cinder-circuit/game.js](/Users/seren/workspace/poong-game/playables/cinder-circuit/game.js) on `buildWave6ChassisBreakpointChoices(...)` using the doctrine-filtered `supportInstallWave = SUPPORT_SYSTEM_START_WAVE` list to choose one real `systemChoice`, then return `bayUnlock + systemChoice` cards for the consolidated path so `applyForgeChoice(...)` installs that rider immediately and the player gets `Wave 6-8` proof laps with a visible support silhouette. Keep [playables/cinder-circuit/tools/cinder-circuit-smoke.mjs](/Users/seren/workspace/poong-game/playables/cinder-circuit/tools/cinder-circuit-smoke.mjs) asserting that the `Wave 6` rider choice now carries `bayUnlock`, `systemChoice`, and a live support payoff. If a future pass moves this support proof back to `Wave 7`, the shipped route will slide back toward weapon-only runs with alternate appetites arriving after the player has already mentally locked the run.
- On the consolidated route, `Wave 8+` support installs should surface only the current doctrine's two preferred amplifier families; if the shipped `12-wave` path starts offering the whole orbitals/sentries/missiles/drone catalog again before one late form is proven, the support layer has widened past the weapon/body spine.
- On the consolidated route, if `Wave 3` already set `architectureForecastId`, `Wave 6` should surface chassis-defense cards only and silently adopt that forecasted doctrine on pick; do not put doctrine names, pursuit hooks, or bay unlocks back on the live `Wave 6` choice itself.
- Late-form card text should sell the weapon/body mutation in sentence one and mention rider/support amplification only as a secondary clause, otherwise support fantasy steals the anticipation again.
- Late ascension forms land better when their base fire geometry and chassis silhouette already look complete at `supportLevel 0`; support should widen or refine the form, not finish it.
- Support-system unlock timing is a high-leverage appetite control knob: if the opening feels too complete, move `SUPPORT_SYSTEM_DEFS[*].forgeWaveMin` and its smoke expectations together before adding or removing whole reward branches.
- Late-break payoff variety lands better when the branch identity is visible by `Wave 9`: mutation should keep open-lane/crownline ownership, aegis should own bastion/refuge cadence, and ledger should own vaultline/caravan greed asks without adding another routing wrapper.
- On the consolidated route, presentation helpers must ignore `afterburn` labels and status copy until after `Wave 12`; otherwise the HUD/roadmap keeps implying the real apex is outside the rerunnable clear even when combat pacing improves.
- Reward reads land better when the default forge/HUD surface shows one oversized form title, one proof capsule, and at most one small rider pill; if a summary needs three labeled rows, it is probably explaining instead of selling.
- On the consolidated route, even shipping-beat labels like `12-Wave Contract`, `Forge Break`, and `Proof Loadout` are too loud on the default reward surface; keep them for internal staging only and let the player-facing forge/HUD read as `main pick + next test + small support hint`.
- On the consolidated route, appetite-first Korean labels like `변이 선택`, `보조 선택`, and `마무리` land better than `Current Form/Main Leap/Next Proof` style admin copy; if a combat-feed or HUD line sounds like route coaching, rewrite it around `pick one big thing, then prove it`.
- Reward readability moves fastest when the recurring three-card contract keeps its labels stable and the choice-specific hero copy/preview rows do the real selling; prefer rewriting `architecture / chassis / bay / greed` cards into `what changes now / what proves it next` language before adding more wrapper UI.
- `Tab` inspect is higher value as a three-lane snapshot than as an expanded build sheet; on the consolidated route, hide bench/upgrades/live telemetry there and show only `주력 변이 / 방호·보조 / 판돈·유틸` plus one current-form/proof card.
