# Critic Session

Purpose: persistent memory for the critique agent.

Use this file to remember:
- recurring structural problems
- repeated false solutions
- what release gate is currently most fragile
- what kinds of critique are becoming repetitive or low value

Keep this short and overwrite stale thinking when the game changes.

## Current Memory

- The main problem is now false build depth: the arena has room to breathe, but the shipped loop still spends `Wave 5-8` proving one gun/chassis line because `supportBayCap` starts at `1`, then rescue logic (`Wave 7` wildcard unlock, `Wave 8` tier-2 bailout) patches the missing second axis. Fragile gate is `Builds`, with `UX/UI` and `Rewards` still feeding the same issue.
- Treat any shipped-facing `mission board / signature / doctrine / contract / proof / rider / roadmap / current machine / Wave 9-12 / Late Break / Afterburn / ascension / black-site` framing as drift. The opener should not ask the player to read future routing, and the forge should not teach closure grammar.
- Highest-value target remains `quiet opener -> Wave 3 weapon leap -> Wave 6 guaranteed support chapter -> Wave 7-8 ownership test -> Wave 8 closure`, with visible transformation arriving on time and without wildcard rescue, bailout installs, or early spectacle creep.
- Reference rule stays `Hades`, `Nova Drift`, and `Brotato`: one craveable reward object, one combat promise, minimal reading, and real reroll hunger from distinct offense/defense/greed paths that are felt onscreen.
- Repeated false solutions: shortening copy without deleting the shell, preserving shadow late-game systems as “future-facing,” adding more support variants before the first real support fork works, or using wildcard/bailout logic to fake build payoff instead of fixing pacing.
