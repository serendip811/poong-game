# Critic Session

Purpose: persistent memory for the critique agent.

Use this file to remember:
- recurring structural problems
- repeated false solutions
- what release gate is currently most fragile
- what kinds of critique are becoming repetitive or low value

Keep this short and overwrite stale thinking when the game changes.

## Current Memory

- The main failure is now shallow back-half structure hidden behind admin wrappers. Docs and UI still drift through `mission board / signature / contract / proof / rider / roadmap / Wave 9-12` language, but the stronger blocker is that shipped progression still reduces most runs to one support bay and a repeated `headline + rider` package. Fragile gate is now `Builds`, with `UX/UI` close behind.
- Treat any shipped-facing `mission board / signature / contract / proof / rider / roadmap / current machine / Wave 9-12 / Late Break / Afterburn / ascension` framing as immediate drift, but also treat fake branch breadth as drift: named routes and closure variants do not matter if the run still cannot produce visibly different offense, defense, and greed ownership by `Wave 6-8`.
- Highest-value target remains `quiet opener -> Wave 3 weapon leap -> Wave 6 support/offense/utility fork -> Wave 7-8 ownership -> Wave 8 closure`. The opener still must stay simple; early cleanup is not permission to front-load missiles, orbitals, or spectacle again.
- Reference rule stays `Hades`, `Nova Drift`, and `Brotato`: one dominant reward object, one combat promise, minimal explanation. Also compare whether their choice structure actually supports reroll hunger; presentation alone is not enough.
- Repeated false solutions: shortening copy without deleting the shell, preserving shadow late-game systems as “future-facing,” adding more named branches without widening the real `Wave 6-8` fork, or polishing combat density while the shipped support economy still caps most runs at one obvious back-half identity.
