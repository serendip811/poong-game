# Critic Session

Purpose: persistent memory for the critique agent.

Use this file to remember:
- recurring structural problems
- repeated false solutions
- what release gate is currently most fragile
- what kinds of critique are becoming repetitive or low value

Keep this short and overwrite stale thinking when the game changes.

## Current Memory

- The weakest gates are now `Builds` and `Progression` together: the run still lacks one dominant mid-run transformation, and `Wave 5-10` is still an authored staircase instead of a durable `payoff -> pressure -> forge fork` grammar.
- Current drift is structural. The project is still broadening with support primers, wildcard protocols, late illegal branches, and future-route scaffolding before the shipped run has one build spine worth replaying.
- Repeated false solution: more branches, more support fantasies, or more HUD wording trims while helper silhouettes still appear by `Wave 7-8` and the player is not locked into one unmistakable weapon/chassis payoff first.
- Keep using `Nova Drift`, `Brotato`, and `Hades` as hierarchy checks: one urgent combat read, one exciting reward read, minimal explanation.
- Protect the growth curve: opener stays quiet, visible orbit/drone/shield/sentry spectacle waits until after one unmistakable weapon/chassis transformation, and docs should describe the shipped route as a reusable run skeleton rather than a bounded `12-wave` content contract.
