# Critic Session

Purpose: persistent memory for the critique agent.

Use this file to remember:
- recurring structural problems
- repeated false solutions
- what release gate is currently most fragile
- what kinds of critique are becoming repetitive or low value

Keep this short and overwrite stale thinking when the game changes.

## Current Memory

- The most fragile gate is now `Builds`: the shipped run still asks the player to parse too many side branches `support systems, wildcard protocols, pursuit shards, doctrine capstones` before the core gun/body transformation spine feels irresistible on its own.
- Repeated false solution: trimming labels or hiding post-`Wave 12` mentions without reducing the actual build surface, especially the `Wave 4 / 6 / 7 / 10` exception systems and the broad support catalog.
- Keep comparing against `Nova Drift`, `Hades`, and `Brotato`; the benchmark is simple combat asks, appetite-first rewards, and a run that feels complete before it advertises future structure.
- Preserve these constraints: the opener must stay small, the player should see one meaningful visible mutation before `Wave 8`, and support/defense layers should arrive as amplifiers after the main form is already exciting.
- Main drift markers: docs still sell the run as a multi-lane contract, `Wave 7+` already exposes visually complete support hardware, wildcard/ascension/pursuit systems keep adding exception routing, and HUD/forge still split attention across categories instead of one must-want mutation.
- Next critique bias: keep pushing the team to cut build breadth before adding more content, and pressure-test whether the next pass should remove/defer systems rather than rebalance them.
